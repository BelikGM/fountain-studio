# Журнал работ

Что сделано в каждой сессии, что осталось, чем проверено. Самая свежая запись —
сверху. Читать первым, когда садишься за проект: отсюда видно, на чём
остановились.

Правило: запись добавляется в конце каждого законченного запроса, вместе с
коммитом и пушем (см. `CLAUDE.md` → «Порядок сдачи работы»).

---

## 23.09.2026 — без объекта: копия настроек, часы, два редактора, «почему не играет», COM-порт, получатели; разбор MADRIX

Работали по списку «Без объекта — по порядку» из `IDEAS.md`, каждый пункт —
свой коммит; сделанное перенесено в раздел 2.1 «Проверить на этом компьютере»
с описанием, как это проверить руками.

**1. Выбор COM-порта списком.** Вместо поля с подсказками — выпадающий список
найденных портов с микросхемой переходника («COM3 · FTDI», «COM4 · CH340»,
«COM12 · CP210x», «COM1 · не USB») в трёх местах: выход USB-DMX у вселенной,
датчик ветра по RS-485, ПЧ насоса. Порт из настроек, которого сейчас нет,
остаётся в списке как «не подключён» и не теряется; последний пункт — «вписать
вручную…». Опрос портов один на все списки сразу. Для снимков добавлены
`FOUNTAIN_TEST_COM_PORTS` в движке и поля `engineEnv`/`lines` в сценарии
`ui-shots` — на машине разработчика переходников нет.

**2. Резервная копия настроек ПРОГРАММЫ.** «Настройки» → «Резервная копия
настроек программы»: один файл с лицензией, токеном бота, настройками движка и
списком недавних объектов — того, чего НЕТ в копии объекта. Внутри архива
`ЧТО-ЭТО.txt` с объяснением и предупреждением, что там токен. Восстановление —
с проверкой: копия объекта и битый JSON рабочие настройки не затирают. Панель
копий объекта переименована в «Резервные копии объекта», чтобы две копии не
путались. Самопроверка `appbackup-test` (19/19).

**3. Расписание и прыжки часов.** Планировщик ждал ТОЧНОГО совпадения секунды:
при прыжке 20:59:58 → 21:00:07 (сверка времени, выход из сна, перевод часов)
вечерняя программа не запускалась вовсе. Теперь прыжок замечается: догоняется
ПОСЛЕДНЯЯ пропущенная запись (записи — переходы, а не очередь), одиночное шоу
после долгого простоя заново не поднимается, перевод часов назад не включает
вечернюю программу второй раз. Всё видно в журнале. Самопроверка
`schedule-test` (16/16).

**4. Два редактора одновременно.** У объекта появилась версия: движок метит
автора правки, не принимает правку «поверх чужой» и говорит редакторам друг о
друге (полоса на всех вкладках + запись в журнале). Попутно исправлено то, из
-за чего чужая правка МОЛЧА пропадала: редактор считал «сколько своих правок в
пути» и съедал чужую, пришедшую в этот момент. Самопроверка `editors-test`
(13/13), плашки сняты на изолированном движке.

**5. Экран «почему ничего не играет».** Ссылка «почему не играет?» в строке
внизу окна: главный ответ крупно и причины по важности (авария, пауза, стоп по
расписанию, не открыт объект, нет лицензии, пустое расписание, вселенная без
выхода, ветер, нет ffmpeg) с кнопкой «Перейти». Разбор чистый, в
`shared/whyquiet.ts`; самопроверка `why-test` (25/25).

**6. Перетаскивание порядка во всех списках** (секвенсоры, шоу, плейлисты,
расписания) — общий механизм `components/DragOrder`: тащим за ручку «⠿»,
переставляем в полном массиве, чтобы фильтр слева не перемешивал скрытое. У
расписаний в подсказке сказано, что порядок решает спор при совпадении времени.

**7. Несколько получателей уведомлений.** Дежурному — аварии, начальнику —
только отчёт. Номер чата человек про себя не знает, поэтому движок запоминает
тех, кто писал боту, и в настройках их добавляют одним нажатием. Копии идут без
кнопок: команды движок принимает только из главного чата. Заблокировал бота
один — остальным доходит. Самопроверка `telegram-test` (94/94).

**8. Жалоба «на Отладке значения падают» — разобрано.** Причина не в экране:
поверх ручного управления работает аварийное отключение (каждый такт гасит
насосы и клапаны), стоп по расписанию (гасит всё) или движок вообще не
отвечает. Теперь на «Отладке» об этом написано красной полосой с причиной и
кнопкой «Перейти». Сами тест-генераторы проверены отдельно и работают верно:
«Волна» гоняет значения, область «Свет» трогает только светильники, «Выключен»
возвращает в 0.

**9. Связь по RDM видна в списке приборов** — «✔» отвечает, жёлтый «⚠» нода не
видит, в подсказке написано, что проверять (питание, кабель, терминатор, умеет
ли нода RDM, не сменился ли UID). У приборов без привязки к RDM значка нет:
там связь проверить нечем.

**10. Разбор MADRIX RADAR** (экраны заказчика + их обучающие видео). Транскрипты
YouTube достать не вышло — отдаёт «подтвердите, что вы не робот»; разбирали по
официальной документации `help.madrix.com/radar`. В `IDEAS.md` заведён раздел
«Подсмотрено у MADRIX RADAR»: снимок конфигурации приборов (.rsx) и возврат из
него, патч-редактор с автоадресацией перетаскиванием, датчики приборов с
нормами и историей графиком, правила событий с уровнями и действиями, отчёт
«даже если событий не было», поиск с указанием колонки, настройка колонок,
показ/скрытие и перетаскивание панелей с сохранением раскладки, светлая тема.
В пункт автопостановки записаны требования заказчика: разбор трека с частотой
до 50 мс, глубокая синхронизация воды и света, самокритика по измеримым
признакам.

**Проверено:** `typecheck`, `build`, `smoke`, `appbackup-test` 19/19,
`schedule-test` 16/16, `editors-test` 13/13, `why-test` 25/25,
`telegram-test` 94/94; снимки и DOM — на изолированном движке (порт 9531),
рабочие данные не трогались.

**Не сделано / следующее:** цельное тело моторной насадки в 3D; автопостановка
(большая задача, ждём видео объектов заказчика под конкретные треки);
английская версия; `ПЛАН-ПРОВЕРКИ.md` — пошаговый план проверки всего от нуля;
пункты из раздела MADRIX.

---

## 22.09.2026 (поздний вечер) — расписание-переходы; датчик ветра Musidora и 0–10 В / 4–20 мА

**1. Датчик ветра — вопросы заказчика и что нашлось.** Прочитан паспорт
Musidora «Wind» (musidora.ru/SALES/AnemometrPasp.pdf; PDF пришлось разбирать
своим скриптом — текст в нём сжат): анемометр на столбе, три провода (чёрный —
общий, коричневый — +12 В, синий — выход), блок с питанием 220 В и ручками
«Уровень» и «Эффект», выход 0–10 В — прямо на частотники, мимо компьютера.
FontanPlay ветра не видит; мы через USB-DMX тоже не увидим — DMX идёт только к
приборам. Ответ на «MQTT — это 4–20, а Modbus — 0–10?»: нет, это разные
уровни (сигнал по проводу и то, как число доходит до компьютера); аналог в
компьютер — только через модуль «аналог → Modbus». В коде: у датчика Modbus
появилось «При безветрии» (ноль шкалы 4–20 мА — не ноль регистра), а сигнал
ниже нуля шкалы — «обрыв линии», показание держится, в ноль не падает. В
справке: как подключать, и что не снижать струи дважды (блок Musidora и
программа).

**2. Расписание переделано по описанию заказчика.**
- Каждая строка — **переход**: прежнее останавливается само, ручные ползунки
  «Отладки» и тест-сигналы сбрасываются, пауза снимается.
- Действия: макрос, группа, шоу, плейлист, сцена, **«Пауза — замереть»**,
  **«Стоп — в покой»** (горят сцена покоя и служебный свет, если заданы),
  **«Выключить — всё в 0»** (гаснет всё, включая их, до следующего включения
  записью или руками; внизу окна «выключено по расписанию»). «Стоп» и
  «Выключить» оставлены оба, потому что делают разное.
- **Переход**: «сразу» или «гашение N с» — всё в 0 на N секунд, потом запуск.
- **Несколько расписаний** с галочкой «активно»; «Копировать» / «Вставить»
  (в заполненное — вопрос, прежние записи удаляются), «Дублировать» (копия
  выключена, чтобы не столкнуться с оригиналом).
- **Коллизии**: две записи на одну секунду в один день (и между активными
  расписаниями) — красным, сверху список; движок исполняет первую, о второй —
  предупреждение в журнале.
- **После перезапуска** движок включает то, что по расписанию должно идти
  сейчас (одиночное шоу не повторяет) — сбой питания в 14:00 больше не
  оставляет фонтан тёмным до вечера.
- Плейлист «с начала / с места остановки» уже был (вкладка «Плейлисты»).
  Честно: место остановки хранится в памяти движка, после перезапуска —
  с начала; записано в IDEAS.
- Старые объекты открываются с одним активным расписанием «Основное».
- Вёрстка: на 1280 px таблица не влезала (крестик за краем окна) — название
  записи ушло второй строкой под действие, дни плотнее.

Заодно: «Выключено» не доходило до строки состояния — сервер слал состояние
только при смене версии воспроизведения; теперь следит и за этим.

**Проверено:** typecheck, build, smoke (+11 про расписание: переход, сброс
ручного, «Выключить» и выход из него, гашение, коллизия с предупреждением,
выключенное расписание, «что должно идти сейчас», перенос старого объекта),
projects 58, dirty-guard 22, license 57, musidora 67, failsafe 22, eventlog 11,
wind 159 (+3: «при безветрии», обрыв 4–20 мА с удержанием показания), worker
76, telegram 84, audio 62, lines 13. Снимками на изолированном движке:
коллизия (одна пара, а не две), копирование и вопрос при вставке, два
активных расписания, «выключено по расписанию» внизу окна через запись на
4 секунды вперёд, таблица на 1280 px; панель «Ветер» с новым полем.

**Не проверено:** настоящий модуль «аналог → Modbus» и блок Musidora — их нет
в руках.

---

## 22.09.2026 (вечер, второй заход) — ветер: источник и датчик; «Громкость и тембр музыки»; макросы; iPhone

Четыре пункта заказчика.

**1. «Звук плейлистов и расписания» — у расписания звука нет,** звук у музыки.
Панель называется «Громкость и тембр музыки»; во вступлении — что это музыка
шоу, которую движок играет сам, когда шоу запускает плейлист или расписание.

**2. iPhone — записано в IDEAS отдельной огромной задачей:** «Отладка», «Шоу»,
«Расписание» без разработки шоу; проверить оборудование, показать заказчику,
открыть большой объект. Сразу записано, от чего зависит сама возможность:
сторонних USB-драйверов (FTDI) на iPhone нет — DriverKit только на iPad с
чипом M, значит DMX кабелем — только интерфейс, который iOS понимает сам (надо
найти), а без оговорок — Art-Net/sACN по Wi-Fi или через USB-C → Ethernet;
iOS усыпляет приложение в фоне; движок на Node.js на iPhone надо переносить.
Предложено начать с телефона как пульта к движку на компьютере.

**3. Ветер — сделано целиком.** Что было и что стало:
- **Откуда брать ветер** — выбор в «Настройки» → «Ветер» (было — только
  галочка «Включено»): «Не учитывать ветер», «Ручной ввод — проверка без
  датчика», «Датчик по Modbus (RS-485 или шлюз)», «Датчик по MQTT».
- **Датчик по Modbus** (`windsensor.ts`): опрос раз в секунду, функции 03 и 04
  (добавил в драйверы 04 — многие анемометры отдают скорость input-регистром),
  масштаб «единиц на 1 м/с», необязательный регистр направления. Подключение
  — USB-переходник RS-485 или шлюз TCP; **пул подключений Modbus вынесен из
  насосов** (`modbuspool.ts`): датчик на той же линии RS-485, что частотники, —
  одно подключение, одна очередь запросов, а не два открытия одного порта.
- **Датчик по MQTT**: топик на брокере из «Внешних пультов», число или JSON.
- **Датчик пропал** (по умолчанию 10 с тишины) — одна авария в журнал и
  Telegram, последнее показание ДЕРЖИТСЯ; вернулся — «снова на связи».
- **Ручной ввод** на «Отладке» — только в режиме «ручной ввод»; при датчике
  там показание датчика. Смена источника или датчика сбрасывает прежнее
  показание.
- **3D и ветер движка — один ветер** (было два: снижение насосов 3D
  показывала, а снос в сторону — только от своего ползунка). При ручном вводе
  ползунок в 3D и есть ручной ввод; при датчике — показывает датчик, крутить
  нельзя, направление от датчика; при «не учитывать» — только картинка.
- **Найденная по дороге ошибка:** состояние ветра уходило в редактор только в
  момент ввода, и «Отладка» навсегда оставалась на «⏳ ждём 10 с подряд», хотя
  насосы уже резались. Теперь движок сам сообщает, когда коррекция вошла в
  силу; на снимке «ждём» через 13 с само сменилось на «струи ограничены до
  87 %».

Ответы на вопросы заказчика про ветер — в справке, раздел «Частые вопросы»:
снижение зависит и от наибольшей высоты струи, и от ТЕКУЩЕГО значения насоса;
ветер по USB-DMX/RDM не приходит, датчику нужен свой канал.

**4. Макросы.** В FontanPlay «макрос» — это наша сцена (статичный) или
секвенсор/группа секвенсоров (динамический). Вкладки «Макросы» нет — это
«Секвенсоры»; теперь это написано в подсказке вкладки, на её пустом экране и в
справке. Ограничений на число нет. Запуск из расписания есть (секвенсор,
группа). Но вопрос вскрыл три настоящих пробела — записаны первым пунктом
IDEAS «Дневная программа»: шоу и секвенсор складываются «кто больше» (в 20:00
вода макроса смешается с шоу), после шоу макрос сам не вернётся, после
перезапуска посреди дня расписание не догоняет текущий режим. До исправления
в справке написано, как обойтись записями расписания («Стоп всё» перед шоу,
секвенсор после).

**Проверено:** typecheck, build, smoke, projects 58, dirty-guard 22, license 57,
musidora 67, failsafe 22, eventlog 11, wind **156** (+30: разбор настроек,
MQTT-показания, поддельный анемометр Modbus TCP — функции 03/04, направление,
общая линия с насосом одним подключением, пропажа с удержанием и одной
аварией, возврат, запрет ручного ввода при датчике, сброс при смене
источника, сообщение редактору о вступлении коррекции), worker 76, telegram
84, audio 62, lines 13. Снимками на изолированном движке с поддельным
анемометром: все режимы «Откуда брать ветер» и их статусы («✔ датчик на
связи: 5,8 м/с, дует с 225°», «✖ не указан COM-порт», «✖ не указан топик»,
«MQTT выключен — включите…»), «Отладка» при ручном вводе и при датчике, 3D при
ручном вводе и при датчике, пустой экран «Секвенсоров», «Громкость и тембр
музыки»; все 13 вкладок на 1366 и 1920 — без обрезанных подписей.

**Не проверено:** настоящий анемометр — только поддельный на TCP; RS-485
через настоящий USB-переходник — нет.

---

## 22.09.2026 (после обеда) — OSC/MQTT с вкладки, английские термины назад, IDEAS по-новому

Четыре пункта заказчика.

**1. Общепринятые английские слова — вернуть.** «уд/мин звучит даже не очень;
не обязательно всё переводить — это общепринятые названия». Прошёл по всему,
что я переводил, и сверил с тем, как заказчик пишет сам (в прошлых сообщениях
у него «BPM», «ползунок», «пресеты», и он сам просил «тип оборудования»
вместо «профиля»). Вернул: **BPM**, **Mute** (кнопка дорожки в «Шоу»),
**Blackout** («Blackout — погасить всё» в списках клавиш и пультов, «⚠
BLACKOUT» на экране оператора, в меню бота), **Art-Net нода** (вкладка
«Диагностика», «Поток», журнал, Telegram, подсказки «Настроек»),
**multicast** у sACN. Оставил по-русски то, что звучит естественно и так же
точно: ползунок, резервная копия, тип прибора, «Низкие/Высокие», «такт».
Рядом с каждым английским словом — подсказка по-русски. Правило записано в
`ТЕРМИНЫ.md` (п. 7).

**2. «Звук вечерней программы» — неверное название:** шоу играют и утром, и
днём. Панель называется «Звук плейлистов и расписания» — это ровно то, когда
движок играет музыку сам (прослушивание на «Шоу» идёт через редактор, на него
панель не влияет). Заодно убрал «вечер» из подписи вкладки «Плейлисты», из
тарифа, из пустого экрана плейлистов, из предупреждений о звуке и из
руководства. Правило «время суток не зашивать в названия» — `ТЕРМИНЫ.md`
(п. 8).

**3. IDEAS пересобран на три раздела:** что делаем дальше; сделано, но руками
не проверено (всё за 15–22.09); сделано давно и проверено. Раздел 2 — чек-лист
по вкладкам слева направо: 38 пунктов «☐», у каждого где смотреть, что
сделать и что должно быть; отдельно 2.2 — то, что проверяется только на
объекте, с железом (тексты прежнего раздела 2 перенесены как есть). Сборка —
разбором прежнего файла по строкам, со сверкой: из 84 прежних пунктов ни один
не потерян, 12 стали пунктами чек-листа или переехали.

**4. OSC и MQTT включаются с вкладки.** Заказчик не понял пункт в IDEAS —
по-простому: OSC — это команды с планшета (приложение TouchOSC) по Wi-Fi,
MQTT — из умного дома или диспетчерской через брокер. Раньше включить их
можно было только правкой файла `app-config.json` с перезапуском движка,
вкладка лишь писала «выключен — включает наладчик в файле». Теперь на
вкладке «Внешние пульты» панель «Подключение»: галочка и порт OSC, галочка,
адрес брокера, порт, префикс, логин и пароль MQTT, «Применить». Применяется
на ходу, без перезапуска и без остановки шоу; меняется только то, что
поменялось. Рядом — правда о состоянии: «✔ принимает команды на порт 8000»,
«✖ порт 8000 занят другой программой», «✔ на связи с брокером», «✖ не указан
адрес брокера». Пароль брокера в редактор не уходит — только «задан / нет».
Настройки пишутся в настройки программы дописыванием (адрес отзыва лицензий
и прочее не стираются). Панель поставлена на саму вкладку, а не в
«Настройки», как было записано в IDEAS: состояние пультов показывалось
именно там, туда человек и пойдёт.

**Проверено:** typecheck, build, smoke (с новыми проверками: OSC переезжает
на другой порт на ходу, старый порт больше не слушается, MQTT выключается и
включается обратно, занятый порт называется словами, пароль принят, но в
статусе его нет, пустой пароль — убран, всё записано в файл настроек во
временной папке), projects 58, dirty-guard 22, license 57, musidora 67,
failsafe 22, eventlog 11, wind 126, worker 76, telegram 84, audio 62,
lines 13. Снимками на изолированном движке: «Подключение» (включение OSC,
MQTT без адреса, MQTT без брокера, пароль «задан», пароля нет на странице),
«Blackout» в списках клавиш и пультов, экран оператора, «Mute» в шапке
дорожки, «Art-Net ноды», «multicast» у sACN, «Звук плейлистов и расписания»;
все 13 вкладок на 1366 и 1920 — без обрезанных подписей. Рабочие
`fountain.project.json` и `fountain.config.json` не менялись.

**Не проверено:** OSC с настоящего планшета и MQTT с настоящим брокером —
только с тестовым UDP-отправителем и поддельным брокером в смоуке.

---

## 22.09.2026 (день) — сплошная вычитка закрыта, тембр под колонки

Запрос: довести до конца пункты «Сейчас» — сплошную вычитку интерфейса и
эквалайзер (низ/верх); «частота» уже была разобрана и не делается.

**Вычитка.** Пройдены все 13 вкладок по четырём вопросам из IDEAS (понятно ли
фонтанщику, отвечает ли на «что делает», хватает ли пояснения и видны ли
диапазоны без мыши, влезает ли). Открытые панели и диалоги — сценариями снимков
на изолированном движке (9531), сообщения движка и тексты Telegram — по
выписке всех строк через компилятор TypeScript, латинские подписи — отдельной
выпиской. Слова сведены к одному словарю: «объект», «узел Art-Net»,
«резервная копия», «тип прибора», «вид», «ползунок», «переход» /
«нарастание» / «затухание», «Погасить всё», «Выкл» у дорожки шоу, «уд/мин»,
«вся сеть» у sACN — всё записано в `ТЕРМИНЫ.md` вместе с правилом чисел
(`num` / `countOf` / `durationRu` в `shared/src/ru.ts`).

Нашлось не только в словах: двойное здоровье насоса с оборотами в «Гц»; имя
вместо номера в аварии насоса; ложное «хранятся последние 20 копий»; неверное
место токена Telegram в подсказке; отсылка журнала событий на «Поток» вместо
«Диагностики»; статус связи за краем окна; в «Шоу» крестик дорожки под шкалой
(шапка дорожки 216 → 252 px — в неё не влезали «мс», «Выкл», 🎚, ↑, ↓, ✕).

Инструмент снимков научился ловить срезанное по ширине и ряд кнопок шире своего
места: крестик в «Шоу» старая проверка пропустила. Новую проверил на старой
ширине — находит; на новой — чисто. Все 13 вкладок на 1366 и 1920 и все
сценарии панелей с новой проверкой — чисто.

Руководство (`РУКОВОДСТВО.md`, оно же «Справка») отставало: вкладки «Пульт» и
«Приборы», BLACKOUT, фейдеры, профили, «Синус/Бегущая/Пила», «Снять значения
с пульта». Переписано по нынешним подписям, добавлены разделы «Поток» и
«Настройки»; все названия в кавычках сверены с интерфейсом выпиской.

**Тембр.** «Настройки» → «Звук вечерней программы»: «Низкие» (bass, 100 Гц) и
«Высокие» (treble, 6 кГц), от −12 до +6 дБ, «Тембр как в файле». Движок
получает значения по `setAudioVolume`, хранит в настройках программы, ffplay
получает фильтры. Замер ffmpeg поймал граблю: в 16-битном wav фильтр bass сам
срезал пики о потолок («clipping 2500 times»), и ограничитель после него уже
ничего не спасал. Теперь перед тембром `aformat=sample_fmts=fltp`, в конце
мягкий `alimiter`. Самопроверка меряет пик через `astats` (volumedetect выше
0 дБ не показывает) и сам срез.

**Проверено:** typecheck, build, smoke, projects 58/0, dirty-guard 22/0,
license 57/0, musidora 67/0, failsafe 22/0, eventlog 11/0, wind 126/0,
worker 76/0, telegram 84/0, audio 62/0, lines 13/0. Снимки панели звука на 1600
и 1280, круг «ручка → движок → ручка» и сброс проверены через DOM. Рабочие
`fountain.project.json` и `fountain.config.json` не менялись (даты 15.09 и
25.08).

**Не проверено:** тембр на колонках объекта — только замером; таблица
«Поток RDM» с приборами — на изолированном движке RDM-приборов нет.

**Записано в IDEAS:** включать OSC/MQTT из интерфейса (сейчас только файлом);
английская версия целиком — после того, как русская отточена, переключатель
флагом в «Настройках» (флаги картинками: Windows не рисует эмодзи-флаги).

---

## 19.09.2026 — тариф Pro: Шоу и Плейлисты только смотреть и запускать

Последний пункт из «Сейчас». Теперь всё, что можно сделать без объекта,
сделано.

**Главное решение — где стоит запрет.** Не на кнопках, а в ОДНОЙ точке: там,
где вьюха берёт `updateProject`. В ShowView кнопок правки десятки, пропустить
одну легко, и тогда правка ушла бы в проект тихо — а это уже утечка тарифа.
Через запрет в одной точке данные проекта не изменятся даже если какая-то
кнопка осталась на виду.

**Само правило — в `canEditShows(access)` в `shared/license.ts`,** а не в
разметке. Две причины. Это условие продажи, и проверяться оно должно
самопроверкой: в `license-test` теперь три строки — Max правит, Pro нет, без
лицензии нечего. И второе: спрашивают его в нескольких местах, разойтись им
нельзя.

**Что спрятано:** создание, дублирование и удаление шоу и плейлистов; создание
шоу по шаблону; добавление и удаление дорожек; добавление пунктов плейлиста;
загрузка аудио; правка имён, режимов и длительности. Отключено перетаскивание
блоков, точек огибающих и дорожек.

**Что оставлено — и это половина работы:** выбор шоу и плейлиста, пуск, пауза,
стоп, перемотка по линейке, масштаб, рендер в видео. Перемотку я сначала
отключил вместе с остальным перетаскиванием и вернул: линейка — это просмотр,
а не правка, и без неё вкладка стала бы бесполезной.

В обеих вкладках написано, почему нельзя править и что нужен Max: пустая
панель без объяснения читается как поломка.

**Проверено:** `license-test` 57 проверок (было 54), typecheck, build, smoke
2 прогона, projects 58, wind 126, worker 66, telegram 84, failsafe 14,
dirty-guard 22, eventlog 11, musidora 67.

**Не проверено:** глазами в режиме Pro не смотрел — в этой сессии нет
браузерного инструмента, а выдавать себе Pro-лицензию на рабочей машине, чтобы
посмотреть, значило бы трогать рабочие данные. Логика закрыта тестом и
типами, но как ВЫГЛЯДЯТ вкладки под Pro, надо посмотреть глазами — это
первое, что стоит сделать в следующий раз.
## 22.09.2026 (ночь) — снимки своими глазами, плавность «больше — плавнее», звук в дБ, без «линии»

### 1. Проверка интерфейса своими снимками — теперь есть и работает

Заказчик: «Что значит браузерного инструмента нет? Ты должен тестировать всё
непосредственно». Он прав, и отговорка была негодной: в CLAUDE.md давно
написано, как проверять — поднять окно Electron на `127.0.0.1:5180` и снять
кадр, а Electron есть в самом проекте. Сделано:

- `scripts/ui-shot.cjs` — окно Electron по сценарию: вкладки и кнопки по
  тексту, прокрутка, снимок (целиком или фрагмент крупно), `fit` — автопоиск
  обрезанных подписей, срезанных по высоте блоков и того, что уехало за край
  окна;
- `npm -w @fountain-studio/engine run ui-shots -- сценарий.json` — то же на
  ИЗОЛИРОВАННОМ движке (порт 9531, своя папка, демо-проект, копия лицензии):
  нажимать можно что угодно, рабочий объект не трогается;
- редактор понимает `?engine=9531` — к какому движку подключаться.

По дороге сам инструмент дважды обманул, и оба раза это поймано сверкой с
DOM: (1) скрытое окно перерисовывалось с опозданием — на снимке галочки нет,
а в DOM она стоит; лечится внеэкранной отрисовкой и перерисовкой перед
снимком; (2) поиск по тексту нажимал обёртку вокруг кнопки с тем же текстом —
шаг писал «нажато», а ничего не происходило; теперь сначала то, что
нажимается, и самый внутренний элемент. Вывод записан в CLAUDE.md: снимок не
заменяет проверку DOM.

Что нашли снимки (и исправлено):
- на окне 1600 px статус связи в шапке уезжал за край («движок подклю»), на
  1366 и 1280 — тоже. Теперь шапка помещается на 1280/1366/1600/1920: вкладки
  плотнее на узких окнах, статус — цветной точкой с подсказкой, номер версии —
  в подсказке к названию;
- **в «Шоу» шапка дорожки была выше своей строки** (48 px при нужных 65), и
  ряд кнопок срезался — вместе с кнопкой 🎚, о которой я писал заказчику как о
  месте, где живёт эффект плавности. Её было просто не видно. Высоты — по
  замеру, ручка перетаскивания — в строку с именем;
- «Вид приборов:» на «Потоке» ломался на две строки;
- «Профиль:» в форме добавления приборов, хотя в таблице тот же столбец давно
  «Тип»; «тик 50 мс» в строке состояния вместо «такт», и подсказка к нему
  всегда говорила «50 мс», даже при другом такте.

Сквозная проверка вселенных снимками (изолированный движок): добавил
вселенную → блок «Применить» подсвечен с текстом, что не применено → на
«Потоке» плашка, выбор «1 / Все» → «Применить» в плашке → «1 / 2 / Все».
Прогон всех 13 вкладок на 1920 и 1366 — без находок в начальном состоянии.

### 2. Слова «линия» в интерфейсе больше нет

Заказчик снова увидел «линию» и спросил, почему не «вселенная». Поиск по всем
вкладкам (текст, подсказки, списки) показал: «Линии N» как имени не осталось,
но слово жило в смысле кабеля — «что уходит в линию», «погасить линию?»,
«Импорт с линии», кнопка «Линия» в секвенсоре. Я вчера оставил это
сознательно, и это было неверно: человек не различает два смысла одного
слова. Заменено на «приборам», «в кабель», «на связи», «вход Art-Net»,
«Переход» — в интерфейсе, руководстве и сообщениях движка (журнал, Telegram).

### 3. Плавность: больше — плавнее, до 10 секунд

Заказчик: «логичнее, чтобы при силе 100 переход длился условно 11 секунд…
и так для всех эффектов; почему 11, проще привязать к 10». 11 секунд ни из
чего не следовали: постоянная времени при силе 1 была взята 2 с произвольно,
а 11 с = 2 с × ln 255. Теперь «плавность 1…100», больше — плавнее, число —
время в десятых долях секунды: 1 — 0,1 с, 10 — 1 с, 100 — 10 с. Одно поле на
оба места, где эффект есть: дорожки шоу и секвенсоры. В секвенсорах заодно
убраны Quick/Rate/Decay и «Сила:» без диапазона — то, что вчера исправил
только в «Шоу». Старые проекты переводятся по времени перехода (сила 10 →
плавность 11, сила 50 → 2, сила 1 → 100); это проверяет сквозной тест на
проекте со старой записью. Правило «число растёт вместе с тем, как поле
названо» записано в ТЕРМИНЫ.md.

### 4. Звук: громкость в децибелах, «частоту» не делаем

По снимку окна FontanPlay («Volume 2,86 db», «Friq 44100 Гц») разобрано по
самой FontanPlay на этом компьютере (строки из FontanPlay.exe и audio.dll,
только чтение): звук там играет BASS, у ползунка «Friq» есть кнопка «вернуть
на середину». Это скорость воспроизведения — ускоряет трек и повышает тон,
портит звук и уводит трек от воды. Не делаем; ответ — в руководстве.

Громкость переделана на дБ: −40…0, шаг 0,5, и «звук выключен» (как «Volume
is OFF» в FontanPlay). Выше 0 дБ нельзя — перегруз в колонках. Громкость
идёт фильтром в той же цепочке, что вырезки монтажа. **Проверено по-
настоящему:** тестовый тон прогоняется через ffmpeg с тем же фильтром, что
получит проигрыватель, — «−6 дБ» даёт ровно −6 дБ, «−20» — −20. Старые
проценты переводятся сами (50 % → −6 дБ). Проверка audio-test: 40 пунктов.

### Проверено

typecheck, build, smoke (+ шкала плавности и перевод старых проектов),
projects 58, dirty-guard 22, license 57, musidora 67, failsafe 22, eventlog 11,
wind 126, worker 76, telegram 84, audio 40 (с замером через ffmpeg), lines
75 + 13. Снимками: вселенные (добавить → плашка → применить), плавность в
«Шоу» и «Секвенсорах», звук в дБ и «звук выключен», шапки дорожек, шапка окна
на 4 ширинах, все вкладки на 1920 и 1366. Рабочий движок жив, в нём две
вселенные (вторую применил заказчик), громкость 0 дБ.

### Не проверено

- Автопроверка подписей смотрит начальное состояние вкладок; открытые
  панели, диалоги и всплывающие подсказки глазами пройдены только те, что
  выше. Остальное — в сплошной вычитке (IDEAS, отметки по вкладкам).
- Громкость в колонках на объекте — ушами не слушал; проверен уровень на
  выходе ffmpeg.
## 22.09.2026 (вечер) — вторая вселенная не видна на других вкладках

**Вопрос заказчика.** Добавил вторую вселенную в «Настройках», ушёл на «Поток» —
там только «1» и «Все». В привязке приборов — только «Линия 1». «Линия 1 — это
первая вселенная или что? Почему при добавлении вселенной она не появляется
везде, где есть выбор?»

### Почему так было

Вселенная была добавлена, но **не применена**. Все вкладки показывают то, что
реально работает в движке, а о неприменённой правке знала только таблица в
«Настройках». Проверено: рабочий движок и сейчас отвечает одной вселенной.
Почему не применили — видно по экрану:

- кнопка «Применить и сохранить» стояла **ниже трёх других панелей** (такт,
  подготовка значений, звук), да ещё под абзацем «кнопка „Применить“ ниже к ней
  не относится»;
- рядом с ней было написано «применение остановит воспроизведение» — на
  такую кнопку не жмут между делом;
- если связи с движком в момент нажатия не было (а движок в тот вечер
  перезапускался от моих правок), редактор всё равно писал «✔ применено» —
  галочку он ставил сам, не дожидаясь ответа.

Отдельно: одна и та же вещь называлась по-разному — «вселенная» в настройках,
«Линия 1» в списках, просто «1» в кнопках. Отсюда вопрос «это первая
вселенная или что».

### Что сделано

**Применение на ходу.** «Применить» больше не останавливает воспроизведение.
Вселенная с неизменным выходом остаётся как есть (тот же открытый порт, те же
ручные уровни), новым открываются выходы, убранным сначала уходит безопасный
кадр. Выходы новой конфигурации открываются ДО изменений: если не открылся
(у Art-Net нет адреса, у адаптера нет порта) — отказ с понятным текстом
«Вселенная 2: Art-Net: не указан IP-адрес узла — впишите его в столбец
„Адрес“», а движок остаётся ровно как был. Часы движка не прыгают и при смене
такта — шоу продолжает с той же позиции.

**«Применено» — только по ответу движка.** Движок отвечает редактору, что
применено и что именно изменилось, или почему нет. Черновик стирается только по
ответу «применено»; нет связи или нет ответа за 5 с — так и написано, черновик
остаётся.

**Неприменённое видно отовсюду.** Кнопка «Применить» — сразу под таблицей
вселенных, вместе с тактом; блок подсвечен, пока есть правка, и в нём написано,
что именно не применено. На всех остальных вкладках висит плашка «Не применено:
добавлена вселенная 2 (FountanPlay, выход 2)…» с кнопками «Применить»,
«Отменить», «В настройки».

**Одно название.** «Вселенная 2» или «Вселенная 2 · Северная чаша», если дать
имя; строит его одна функция `universeTitle`. Где перед выбором уже написано
«Вселенная:» — коротко «2». Автоимена «Линия N» больше не хранятся. «U1:5» в
3D стало «адрес 5» (с номером вселенной, если их несколько). Приборы на
вселенной, которой в движке нет, в «Оборудовании» показаны красным с
подсказкой, а не подменой номера. Правило записано в `ТЕРМИНЫ.md`.

**Мастер нового объекта** заводил вселенные всегда как Art-Net на 127.0.0.1 —
то есть в никуда на объекте с FountanPlay. Теперь по тому же рецепту, что
«+ Вселенная» (такая же, как последняя), и через тот же черновик.

**Руководство** (`РУКОВОДСТВО.md`) исправлено: там было написано, что вселенные
правят Блокнотом в `fountain.config.json`, а они давно живут в папке объекта и
правятся в «Настройках». Добавлен вопрос-ответ ровно про этот случай.

### Что нашлось по дороге — три настоящих бага

1. **Провал значений в 0 при пересборке расчёта.** Замер: при переходе на
   отдельный поток воспроизведение на 50–75 мс падало в 0 (1–2 кадра).
   Касалось и вчерашнего переключения режима подготовки значений — я тогда
   написал «картина замрёт на прежних значениях», и это было неверно. Для
   отсекателей такой провал виден. Исправлено: новый поток подхватывает
   последний кадр прежнего. После исправления — 0 провалов в 16 прогонах.
2. **«Ничего не играет» сразу после пересборки.** Новый поток присылает
   состояние не мгновенно, и движок в этот миг отвечал «ничего не играет».
   Редактор мигал остановкой, а два применения подряд теряли сцену (проверено
   выключением исправления: сцена пропадала, насос проваливался в 67 из 127
   выборок). Исправлено: прежнее состояние держится, пока поток не пришлёт своё.
3. **Движок падал при «Применить» на объекте с Art-Net.** Сквозная проверка
   через настоящий сервер уронила движок: опрос сети запускался дважды подряд,
   и обработчик первого сокета звал `setBroadcast` у второго, ещё не
   привязанного. Ошибка старая; на объектах с FountanPlay опрос выключен,
   поэтому там не проявлялась. Проверка теперь это держит (на старом коде она
   падает ровно с `setBroadcast EBADF`).

### Проверено

typecheck, build, smoke, projects 58, dirty-guard 22, license 57, musidora 67,
failsafe 22, eventlog 11, wind 126, worker 76, telegram 84, audio 26, и новая
`lines-test`: 75 пунктов + 13 сквозных на изолированном движке (порт 9531,
своя папка объекта, Art-Net на 127.0.0.1 и нестандартный порт — рабочий объект
и интерфейс FountanPlay не трогались). Рабочий движок после всего жив, в нём
по-прежнему одна вселенная.

### Не проверено

- Глазами интерфейс не смотрел — браузерного инструмента в сессии нет. Плашка,
  блок «Применить» и новые подписи собираются и типизируются, но влезают ли
  они — не видел.
- Добавление второй вселенной на тот же интерфейс FountanPlay на ходу —
  проверено только на Art-Net. На самом интерфейсе оба выхода идут через один
  USB-порт; что он делает при добавлении выхода к открытому порту, видно только
  на железе. Вынесено в раздел 2 IDEAS.
## 22.09.2026 — потерянная вселенная, гашение при закрытии, громкость, вычитка интерфейса

Шесть вопросов заказчика. По каждому — что выяснилось и что сделано.

### 1. Вселенная пропадала при уходе с вкладки — почему и что теперь

Заказчик добавил вселенную в «Настройках», ушёл на «Поток» посмотреть, как
между ними переключаться, вернулся — вселенной нет. Причина найдена в коде:
черновик правки жил в состоянии самой вкладки, а вкладки в приложении не
прячутся, а РАЗМОНТИРУЮТСЯ целиком (`{tab === 'settings' && <SettingsView/>}`).
Уход стирал черновик молча, и на возврате вкладка перечитывала конфигурацию у
движка. Для человека это выглядит как «программа потеряла мою работу» — и это
худший вид ошибки: ничего не сломалось, просто труд исчез.

Сделан `packages/ui/src/settingsDraft.ts` — черновик живёт рядом с приложением
и переживает уход с вкладки. Осознанно В ПАМЯТИ, а не в localStorage: правка
НЕЗАВЕРШЁННАЯ, и пережить перезапуск программы она не должна, иначе человек
после перезагрузки увидит линии, которых в движке нет, и не поймёт, почему
фонтан молчит. Диалогом «вы точно хотите уйти?» лечить отказались: ходить между
вкладками во время настройки — нормально, как раз чтобы посмотреть результат.

Заодно добавлена кнопка «Отменить правки» (раньше отказаться от начатого можно
было только перезапуском редактора) и переписана подпись состояния: вместо
«воспроизведение при применении будет остановлено» теперь сказано главное —
правки ещё не применены, на объекте не действуют, уйти и вернуться можно.

### 2. «Светильники замирают» — есть ли у нас защита

Разобрано по трём случаям.

**Движок жив, но что-то пошло не так** — закрыто давно: аварийное отключение
следит за тремя бедами (такт встал, выход перестал доставлять, умер поток
расчёта) и кладёт воду в 0, свет — по настройке.

**Программу закрыли** — БЫЛА ДЫРА, и это ровно то, что заказчик видел на
объектах. Порт просто закрывался, а DMX-приёмник держит ПОСЛЕДНИЙ принятый
кадр: картинка оставалась там, где её застали, — отсюда «застыли в непонятном
состоянии». Сделано: перед закрытием порта движок шлёт безопасный кадр и
повторяет его трижды (подтверждений в DMX нет, одиночный кадр может не дойти, а
второго шанса нет — порт закрывается следом). То же самое при смене
конфигурации линий: убранная из настроек линия иначе навсегда застывала на
последнем кадре.

**Питание компьютера пропало или процесс убит** — слать некому, и это НЕ
закрывается программой в принципе. Сторож поднимает движок обратно, но пока он
поднимается, вода стоит. Полностью закрывается только железом на объекте:
приборы с собственным таймаутом DMX или реле по питанию насосов. Заведён пункт
в IDEAS — выяснить, какие приборы на объектах это умеют.

Проверка `failsafe-test` выросла с 14 до 22 пунктов: перехватывается отправка и
смотрится, что именно ушло в линию последним, отдельно — что при выключенном
гашении света вода всё равно падает, а прожекторы остаются.

### 3. Эффект плавности: диапазон и где проверить

Сила 1…100, живёт на вкладке «Шоу» → дорожка вида «блоки» → кнопка 🎚. Сила 1 —
около 11 с до цели, сила 100 — около 0,1 с. Заказчик спросил про диапазон
потому, что в интерфейсе его НИГДЕ НЕ БЫЛО ВИДНО: поле подписано просто «сила».
Исправлено — подпись «сила 1…100». Подсказка кнопки говорила «Зоны эффекта
плавности (Quick/Rate/Decay)»: слова из кода, нарушение ТЕРМИНЫ.md. Переписана.
Подписи «с, с» и «по, с» заменены на «начало, с» и «конец, с». Добавлено, как
проверить: зона на стык двух блоков с разными значениями, запустить шоу и
смотреть вкладку «Поток».

### 4. «Прочее» в Потоке и три участка

«Прочее» — приборы, которым в профиле выбран вид «прочее»: не насос, не клапан
и не свет (дым, вентилятор, реле, пламя). Свободные адреса сюда НЕ попадают —
они не относятся ни к какому виду и видны только под «Все». Подсказка была
замкнута сама на себя («только приборы вида Прочее») — у каждого вида теперь
своя, по существу.

Участки: **Выход** — что насчитал движок; **В кабель** — то же после
переадресации, буквально байты в проводе; **Вход** — чужой Art-Net, пойманный
на линии. Раньше это было только в подсказках под мышью — вынесено во
вступление вкладки, вместе с тем, как этим пользоваться: совпадают «Выход» и «В
кабель» — искать в расчёте, расходятся — виновата переадресация.

### 5. Сплошная вычитка интерфейса — заведена как большая задача

Заказчик потребовал пересмотреть каждую вкладку, раздел, пункт и формулировку.
Повод виден по этой же сессии: за один заход всплыло четыре места с текстом
для разработчика, а не для фонтанщика. Это системное — текст писался по ходу
работы над кодом и ни разу не перечитывался глазами того, кто открыл программу
впервые. В IDEAS заведён пункт первым в разделе «Сейчас», с четырьмя вопросами
к каждой надписи и требованием вести список вычитанных вкладок.

### 6. Звук: можем ли играть треки, громкость

Путаница развеяна: треки играются, и всегда игрались. Пока открыт редактор,
музыку играет сам редактор, и ffmpeg не нужен вовсе. ffplay нужен ТОЛЬКО когда
шоу запускает движок без редактора — по расписанию или из плейлиста. На этой
машине ffplay найден (проверено: движок отвечает `audioReady: true`).

Громкости не было вообще — ffplay запускался с уровнем файла. Сделана: 0…100 %
в настройках движка, панель «Звук вечерней программы» в «Настройках» (тестировщик
в собранном приложении в файл настроек не залезет — та же причина, что и у
переключателя подготовки кадров). Выше 100 % не пускаем: усиление сверх
исходного уровня даёт хрип в колонках. Идущий трек при смене громкости НЕ
обрывается — новое значение подхватит следующий: обрыв посреди вечерней
программы это скачок звука и уехавшая от музыки вода. В той же панели прямо
написано, нашёлся проигрыватель или нет, с командой установки.

Сборка аргументов вынесена в чистые `clampVolume`/`playArgs` — чтобы это можно
было проверить тестом, не запуская настоящий проигрыватель. Заведена проверка
`npm -w @fountain-studio/engine run audio-test` (26 пунктов), вписана в CLAUDE.md.

**Про «частоту» НЕ СДЕЛАНО осознанно.** Заказчик попросил её вторым параметром,
сам оговорившись, что не знает, как она задаётся. Прочтений три, и это три
разные работы: тембр (эквалайзер) — дёшево и безвредно; скорость (темп) —
ОПАСНО, таймлайн шоу считается в миллисекундах и растянутый звук уедет от воды;
частота дискретизации — свойство файла, менять её при воспроизведении смысла
нет. Заведено пунктом в IDEAS, нужен ответ заказчика.

### Проверено

typecheck, build, smoke, projects 58, dirty-guard 22, license 57, musidora 67,
failsafe 22 (было 14), eventlog 11, wind 126, worker 76, telegram 84,
звук 26 (новая). Движок поднят, по WebSocket проверено, что новые поля
конфигурации доходят до редактора: громкость 100, проигрыватель найден.

### Не проверено

Глазами интерфейс не смотрел — браузерного инструмента в сессии нет. Новые
панель звука, кнопка «Отменить правки» и переписанные подписи собираются и
типизируются, но как они выглядят и влезают ли подписи — НЕ ВИДЕЛ. Гашение при
закрытии проверено тестом на перехвате отправки, на настоящем железе — нет.
## 21.09.2026 — фактическая правка про клапан, термины, понятная «сила» эффекта

**Главное: я ошибся в факте, и заказчик это поймал.** Я писал в нескольких
местах «клапан на объекте идёт 0,5–0,7 с» и обосновывал этим, почему задержка
предрасчёта незаметна. Это неверно. Клапан переключается быстро: цикл 300 мс —
рабочий, и подбивы на нём уже отчётливо видны, а отсекатели (электромагнитные)
режут воду почти мгновенно — ими делают очередь «как из автомата». 0,5–0,7 с —
это рабочий ЦИКЛ подбивов (открыт/закрыт), и в 3D он стоит правильно; я перепутал
цикл эффекта со временем хода механизма.

Ложное обоснование убрано из четырёх мест (`config.ts`, `playbacksource.ts`,
`worker-selftest.ts`, `framemode.ts`), в 3D формулировка уточнена, чтобы её не
прочитали как время хода. Правильное обоснование задержки другое и честнее:
она касается ТОЛЬКО команды человека и НЕ касается уже идущего шоу — его блоки
срабатывают по своему таймлайну, поэтому быстрая работа клапанами не страдает.
Это ПОСТОЯННЫЙ сдвиг одной команды, а не дрожание ритма.

**Заведён `docs/ТЕРМИНЫ.md`.** Заказчик заметил, что названия режимов —
«обычный режим», «старый режим», «как программа работала раньше» — написаны для
меня, а не для человека, открывшего программу впервые: он не знает ни про какие
прежние версии, и такие названия читаются как недоделка. В файле шесть правил
именования (название говорит ЧТО делает, а не когда добавлено; сравнение всегда
с явным «чем»; десятки и сотни миллисекунд пишем миллисекундами; не обосновывать
свои решения свойствами чужого железа, которых никто не мерил) и таблица
принятых терминов.

**Режимы переименованы** по этим правилам: «Предрасчёт в отдельном потоке»,
«Отдельный поток без предрасчёта», «Расчёт и отправка одним потоком». В
объяснениях убрана история разработки, «0,1 с» заменено на «100 мс», и сказано
точно, с чем сравнивается задержка: «на 100 мс позже, чем нажата кнопка».

**Эффект плавности: «сила 1…100» теперь переведена в секунды.** Фонтанщик
думает «пусть гаснет за секунду», а не «сила 10». Добавлена `smoothReachSec`, и
рядом с полем силы стоит «≈ N с до цели». Сверено прогоном по тикам: сила 10 →
1,1 с (прогон 1,15), сила 1 → 11,1 с (прогон 11,10). Режимы эффекта названы
по-русски: «Плавно вверх и вниз», «Плавно только вниз» вместо Rate/Decay.

**Замерен вопрос заказчика про отправку** (зависит ли частота отправки от того,
как часто меняются значения). Прогон с подменённым выходом-счётчиком, 3 с:

| Что играет | Отправлено | Из них отличались от прошлого |
|---|---|---|
| сцена меняется раз в 1000 мс | 60 кадров (20/с) | 4 |
| сцена меняется раз в 50 мс | 60 кадров (20/с) | 59 |
| ничего не играет | 60 кадров (20/с) | 1 |

То есть отправка идёт 20 раз в секунду ВСЕГДА и от содержимого не зависит: это
keep-alive линии. Повторы кадров — норма DMX, а не потеря.

**Уточнены две задачи в IDEAS.** Пункт про ffplay переписан человеческим языком
(движок запускает маленький проигрыватель для аудиофайла, который человек сам
прикрепил к шоу; никакой «своей» музыки программа не подбирает; без ffmpeg
вечерняя программа отыграет в тишине). Добавлен большой пункт про автопостановку:
сейчас она слышит только громкость, высокие частоты и всплески, а темп считает,
но в расстановку не пускает — залпы ставятся по всплескам, а не по долям.
Перечислено по важности, чего не хватает: класть события на доли, размечать
части трека, менять плотность событий от темпа, выбирать цвет по характеру
звука (спектральный центр уже считается, но не используется), раскладывать волну
по кольцам фонтана вместо «первому и второму прибору».

**Проверено:** typecheck, build, сборка бандла воркера, smoke 2 прогона,
projects 58, license 57, wind 126, worker 76, telegram 84, failsafe 14,
dirty-guard 22, eventlog 11, musidora 67.

**Не проверено:** глазами интерфейс не смотрел — браузерного инструмента в
сессии нет.
## 20.09.2026 — IDEAS пересобран по готовности, новые узкие места

Заказчик: «проверь файл заново — что не сделано вообще, что сделано якобы на
100 %, но, возможно, всё не совсем так; и вынеси отдельным списком то, что
сделано, но надо проверить на объекте». Справедливо: раньше «сделано» и
«сделано, но никто не видел этого на живом фонтане» лежали вперемешку, и по
списку нельзя было понять, что везти на объект.

**Файл разделён по ГОТОВНОСТИ, а не по темам, и именно в таком порядке:**
1. не сделано или сделано не полностью;
2. сделано, но надо проверить на объекте;
3. сделано и проверено.

Тексты пунктов переносились как есть, а не перепечатывались: при
перепечатывании теряются детали, ради которых их и писали. Пересборка сделана
разбором файла, а не руками, и сверена по числу пунктов.

**В раздел 2 переехало девять групп** — всё, что закрыто самопроверками, но не
видело настоящего фонтана. Для каждой написано НЕ «проверить», а что именно
делать и как понять, что не так. По важности сверху вниз:
· новая схема расчёта кадров (сверка света с музыкой — главное);
· ветровое ограничение (пороги по высотам, форсунки у борта);
· команды и кнопки в Telegram (нажатие человеком не проверялось ни разу);
· аварийное отключение (выдернуть кабель на работающем фонтане);
· насосы по Modbus и RDM (всё писалось против выдуманного оборудования);
· выход USB-DMX — самая важная проверка из всех;
· папка объекта на другой машине;
· автозапуск Windows на машине объекта;
· тариф Pro — глазами вкладки никто не видел.

**Шесть новых задач**, узкие места которых раньше в списке не было:
· **не найден ffplay** — программа молча играет воду и свет БЕЗ музыки, а это
  ровно то, ради чего всё делается; надо проверять при старте и говорить прямо;
· **резервная копия настроек программы** — лицензия и токен бота в копию не
  попадают: умер диск, объект восстановится, а лицензию заводить заново;
· **перевод часов и расписание** — что будет с записью «21:00» в ночь перевода,
  не проверялось; в худшем случае вечерняя программа запустится дважды;
· **два редактора одновременно** — правки последнего затрут правки первого
  молча;
· **экран «почему ничего не играет»** — причин с десяток, и сейчас их надо
  искать по разным вкладкам;
· **экономия расчёта на статичной картине** — идея заказчика. Записал ВМЕСТЕ С
  замером, который говорит не делать её сейчас: расчёт кадра занимает 0,03 мс
  на обычном объекте и 1,76 мс на заведомо избыточном при бюджете 50 мс, то
  есть запас в 30–1500 раз. Сложность вырастет, выигрыша не будет.

**Попутно проверил вопрос заказчика про длительность шага секвенсора:** минимум
50 мс, ВЕРХНЕГО ПРЕДЕЛА НЕТ. То есть 10 секунд и больше уже можно, отдельной
задачи не нужно.

**Раздел «Сейчас» пуст** — и это правда, а не оформление: всё, что можно
сделать без объекта, сделано. Дальше либо везти на объект и разблокировать
раздел 2, либо брать задачи из «Потом», которые запуск не приближают.

**Проверено:** число пунктов до и после пересборки совпало (69), ни один текст
не потерян; плюс полный набор самопроверок.
## 20.09.2026 — переключение без остановки и человеческие формулировки

Заказчик посмотрел панель и сказал две вещи, обе по делу.

**Первое: формулировки были написаны для меня, а не для человека,** который
впервые открыл программу. «Кадр в линию уходит каждый такт, здесь — где он
считается» — это жаргон. Переписал всё на язык объекта: панель теперь
называется «Подготовка значений для приборов», а варианты — «Готовить заранее
(обычный режим)», «Считать в момент отправки», «Всё одной очередью (старый
режим)». В пояснении сказано, что программа 20 раз в секунду считает значение
каждому насосу, клапану и прожектору и шлёт их по кабелю, — и что на КАРТИНУ
фонтана настройка не влияет, влияет на ровность отправки и на скорость отклика
на нажатия.

**Второе, важнее: «зачем останавливать всё?».** Я писал, что переключение
останавливает воспроизведение, потому что состояние расчёта живёт внутри
источника. Заказчик возразил: пропустим пару кадров — приборы подержат прежние
значения, фонтан не упадёт, запас по аварийному отключению секунды. Он прав, и
я перепутал две разные вещи: «поток нельзя перенести вместе с его памятью» —
это правда, а «поэтому надо всё остановить» — нет. Состояние, которое важно
человеку, движок знает целиком и может переложить.

Теперь переключение НЕ останавливает воспроизведение: переносятся включённая
сцена, все идущие секвенсоры с их шагом и паузой, шоу с позицией и признаком
«играет/на паузе», пункт плейлиста и пауза всего. Для секвенсоров пришлось
добавить `Playback.startAt` — продолжить с заданного шага; без него секвенсор
прыгал бы на первый шаг, а это видно.

Что всё же сдвигается и о чём честно сказано в окне подтверждения: текущий шаг
секвенсора начинается заново (позиция ВНУТРИ шага не переносится) и на пару
кадров картина замирает на прежних значениях, пока новый источник считает
первый кадр. Для DMX это ничто: протокол состояния, приборы держат последнее
значение.

**Панель свёрнута.** Видно только выбранное и кнопку «Изменить»; развернул —
три варианта с объяснениями; выбрал — окно подтверждения с описанием
последствий. Заказчик попросил именно так: настройка из разряда «поставил и
забыл», случайно ткнуть в список из трёх строк легко, и человек не поймёт, что
только что переключил.

**Проверено:** `worker-test` 76 проверок (было 66). Новый блок — перенос
играющего: медленный секвенсор (шаги по 4 с, чтобы проверка была однозначной)
сохраняет шаг, пауза секвенсора переживает переключение, включённая сцена
остаётся и действительно в кадре, пауза всего переносится. Живьём по WebSocket:
включил сцену, переключил режим туда и обратно — сцена осталась включённой.
Плюс typecheck, build, сборка бандла воркера, smoke 2 прогона, projects 58,
license 57, wind 126, telegram 84, failsafe 14, dirty-guard 22, eventlog 11,
musidora 67.

Попутно поправил проверку, которая цеплялась за конкретное слово в подписи
режима: подписи пишутся для человека и будут переписываться, поэтому проверять
надо, что подпись берётся из списка, а не что в ней есть слово «Заранее».

**Не проверено:** своими скриншотами панель не смотрел — нет браузерного
инструмента.
## 19.09.2026 — переключатель подготовки кадров в настройках

Заказчик: «тестировщик не сможет в собранном приложении зайти в
app-config.json и изменить playbackWorker — добавь переключатель в
настройках». Справедливо: до этого файла в установленном приложении и правда
не добраться, а на объекте сравнить «как сейчас» с «как было раньше» — первое,
что понадобится.

**Сделано: «Настройки» → «Подготовка кадров»,** список из трёх вариантов:
· «Заранее, в отдельном потоке» — заводской;
· «Сразу, в отдельном потоке» — без запаса;
· «Сразу, в главном потоке» — как работало до 19.09.2026.
У каждого в подсказке написано, что он даёт и чем расплачивается, числами из
замеров, а не общими словами.

**Списком, а не полем с числом — и это осознанно.** Заказчик спросил, имеет ли
смысл дать поле для своих значений глубины. Нет: глубина замерена, и глубже
сотни миллисекунд она не даёт НИЧЕГО (0 % повторов и на 100, и на 200, и на
500 мс), а задержку реакции добавляет. Свободное поле приглашало бы искать
там, где искать нечего, и кто-нибудь поставил бы секунду. Три варианта дают
ровно то, что нужно на объекте.

**Как устроено.** Раскладка «один режим → две технические величины» живёт в
`shared/framemode.ts` — одна на движок и редактор, чтобы они не разошлись.
Движок умеет менять источник расчёта на ходу: `setFrameMode` останавливает
воспроизведение, закрывает прежний источник, поднимает новый и отдаёт ему
проект. Остановка честная и написана в панели: состояние расчёта (где середина
шоу, какой шаг у секвенсора) через смену потока не переносится, а угадывать
его — хуже всего.

Попутно пришлось привести в порядок владение звуком: обработчик «пора играть
шоу» раньше вешали прямо на источник, а источник теперь сменный. Теперь он
живёт у движка, а источники подключаются к нему сами.

**Настройка сохраняется дописыванием, а не перезаписью файла.** В
`app-config.json` лежит и то, чего мы не касаемся: адрес списка отозванных
лицензий, MQTT, порт OSC. Перезапись «своим» объектом стёрла бы их молча, и
на объекте это выяснилось бы через неделю неработающим отзывом. Проверил
живьём: после трёх переключений `license.revocationUrl` на месте.

**Если поток не поднялся** — панель показывает красным: выбрано одно, работает
другое, причина в журнале. Человеку надо видеть правду, а не галочку.

**Проверено:** `worker-test` 66 проверок (было 45): раскладка режимов в обе
стороны, переключение на живом движке всех трёх режимов, остановка
воспроизведения при переключении, повторное присвоение того же режима ничего
не ломает. Живьём по WebSocket: все три режима переключились, в журнале
движка каждое переключение, настройки сохранились правильно. Плюс typecheck,
build, сборка бандла воркера, smoke 3 прогона, projects 58, dirty-guard 22,
license 54, musidora 67, failsafe 14, eventlog 11, wind 126, telegram 84.

**Не проверено:** своими скриншотами панель не смотрел — в этой сессии нет
браузерного инструмента. Вёрстка сделана существующими классами панели
(`form-column`, `field`), подписи короткие, длинные объяснения — в подсказках.
## 19.09.2026 — поток по умолчанию, предрасчёт кадров, порядок задач

Заказчик: объектные задачи — в конец списка (сначала делаем всё, что можно
без объекта); поток расчёта включить по умолчанию («если что, потом вернём»);
предрасчёт делать сейчас, без отдельного подтверждения. Плюс два вопроса — про
влияние железа и про глубину запаса.

**Порядок задач.** Раздел «Ждёт железа или замеров с объекта» переехал в самый
конец, проверка USB-DMX перенесена туда же. Причина прописана в самом файле:
иначе тестировщику придётся ездить по одному пункту за раз, а каждая поездка —
это день.

**Поток включён по умолчанию.** Выключается `{"playbackWorker": false}`.
Самопроверки и смоук с этого дня идут через поток — это важнее, чем кажется:
весь набор теперь проверяет именно тот путь, который поедет на объект. Одна
проверка в смоуке из-за этого упала и была поправлена: она читала состояние
воспроизведения сразу после кадра, а состояние приходит сообщением и отстаёт
на десяток миллисекунд. Для человека незаметно, для проверки — гонка.

**Влияет ли железо объекта — ЗАМЕРИЛ, а не рассуждал.** Время расчёта одного
кадра, такт 50 мс (это весь бюджет):

| Нагрузка | Медиана | 99 % | Запас до такта |
|---|---|---|---|
| 1 линия, 10 приборов, 1 секвенсор | 0,01 мс | 0,02 | 2600× |
| 1 линия, 60 приборов, 2 секвенсора | 0,03 мс | 0,04 | 1300× |
| 3 линии, 120 приборов, 4 секвенсора | 0,17 мс | 0,23 | 219× |
| 4 линии, 160 приборов, 8 секвенсоров | 0,44 мс | 0,58 | 86× |
| 8 линий, 160 приборов, 16 секвенсоров | 1,76 мс | 4,34 | 12× |

Вывод: расчёт НЕ является узким местом ни на какой мыслимой машине. Ноутбук
втрое слабее на обычном объекте оставит запас в 400 раз, а на заведомо
избыточном — в четыре. Разница между машинами будет не в расчёте, а в
ТОЧНОСТИ ТАЙМЕРА и в том, кто ещё мешает системе: антивирус, энергосбережение,
фоновые обновления. Это видно и в прежних замерах: на spinMs 2 джиттер был
6 мс на обеих конфигурациях, на рабочих spinMs 10 — 0,4–0,7 мс. То есть
настройки планировщика решают больше, чем процессор.

**Предрасчёт — сделан, и глубина ЗАМЕРЕНА.** Прежняя запись «0,2–0,5 с» была
не расчётом, а рассуждением про задержку реакции; заказчик справедливо
спросил, для каких данных это посчитано. Теперь есть числа. Прогон под
нагрузкой (4 линии × 160 приборов, 16 секвенсоров, такт 50 мс, по 10 с):

| Глубина запаса | Повторов кадра, без помех | С помехами (главный поток подвисает на 40 мс в 8 % тиков) |
|---|---|---|
| 0 (без запаса) | 91 % | 22 % |
| 100 мс | 0 % | 0 % |
| 200 мс | 0 % | 0 % |
| 500 мс | 0 % | 0 % |

Читается так: БЕЗ запаса кадр на линии почти всегда на такт устаревший —
главный поток спрашивал кадр на «сейчас», а поток успевал посчитать его
только к следующему разу. С запасом 100 мс кадр всегда ровно на свой момент.
Глубже — не даёт НИЧЕГО. Поэтому по умолчанию 100 мс, а не секунда: выигрыш
кончается на сотне миллисекунд, а задержка реакции с глубиной растёт.
Настраивается `{"playbackLookaheadMs": 0…2000}`.

**Про «а если сцена 50 мс» и «а если три вселенные».** Длительность сцены на
глубину запаса не влияет никак: считаются КАДРЫ по такту, а не сцены. Сцена на
50 мс не значит «двадцать сцен в буфере» — она значит, что соседние кадры в
буфере сильнее отличаются друг от друга; стоимость та же. Число линий влияет
только на память: кадр это 512 байт на линию, всё кольцо на 100 мс запаса и
32 линии весит 80 КБ. Памяти тут не жалко вообще — ограничивает не она.

**Три ошибки, которые поймала самопроверка** (и все три были настоящими):
1. Заглушка после «стоп» снималась по подтверждению от потока. А поток
   разбирает команду сразу: его состояние мгновенно становится «ничего не
   играет», тогда как кадры на запас вперёд ещё старые. То есть заглушка
   снималась и в линию возвращалось ровно то, что человек остановил. Теперь
   заглушка держится по времени — весь запас плюс два такта.
2. Позиция шоу в редакторе уезжала вперёд на глубину запаса: состояние
   считалось на самый дальний посчитанный кадр. Перемотал на 1,0 с — показывает
   1,2 с. Теперь состояние считается на момент, который СЕЙЧАС уходит в линию.
3. Сдвиг позиции на глубину запаса я сначала применял ко всем командам с
   позицией. «Запустить шоу с нуля» начинало его с 0,15 с — то есть съедало
   начало, а начало шоу это удар на первую долю. А перемотка на ПАУЗЕ вообще
   не должна сдвигаться: там время не идёт. Теперь сдвиг только у сверки со
   звуком, а начало со звуком сходится иначе — старт звука задерживается на ту
   же глубину, поэтому не теряется ничего.

**Проверено:** `worker-test` 45 проверок (было 29): добавились кольцо и его
адресация по времени, сетка времени, доля повторов под нагрузкой, мгновенный
«стоп» и что заглушка держится всю глубину, мгновенное снятие сцены. Плюс
typecheck, build, сборка бандла воркера, smoke 3 прогона, projects 58,
dirty-guard 22, license 54, musidora 67, failsafe 14, eventlog 11, wind 126,
telegram 84.

Живьём: `npm run dev` перезапущен, движок пишет «расчёт кадра идёт в отдельном
потоке», UI на 5180 отвечает 200, по WebSocket идут кадры (19 за 2 с при
uiFrameMs 100) и состояние воспроизведения. Демо-проект на этой машине уходит
в аварийное отключение — в нём линия настроена на USB-DMX FountanPlay, а
адаптера здесь нет; это штатная работа защиты, а не следствие потока.

**Не проверено:** на живом объекте и в собранном установщике целиком. Сверка
света со звуком проверена только расчётом и логикой — послушать на объекте
надо обязательно, это самое заметное, что может испортить предрасчёт.
## 19.09.2026 — расчёт кадра в отдельном потоке (worker_threads)

Заказчик выбрал «делать сейчас». Это последняя крупная перестройка ядра из
оставшихся, поэтому ниже подробно: что унесено, что нет и почему.

**Что унесено в поток.** Только воспроизведение: сцены, секвенсоры, шоу,
плейлисты, огибающие. Это самая тяжёлая часть и единственная, которая
целиком определяется временем.

**Что НЕ унесено — и это принципиально.** Всё, что человек крутит руками:
ручная консоль, ветер, служебное освещение, калибровка, тест-генератор,
аварийное отключение, переадресация. Они применяются в главном потоке поверх
присланных уровней. Иначе фейдер оператора начал бы отставать на задержку
обмена между потоками, а «стоп» доходил бы до линии позже, чем нажат.

**Как передаются кадры.** Через `SharedArrayBuffer`, без копирования и без
сообщений. Полукадр прочитать нельзя: перед записью поток увеличивает
счётчик (он становится нечётным), после записи увеличивает снова. Читатель
запоминает счётчик, копирует, сверяет: изменился или был нечётным — значит
прочитано вперемешку, берём прошлый кадр. Ни блокировок, ни ожидания —
главный поток никогда не встаёт в ожидании расчёта, а это и есть цель.

**Почему поток ведёт часы сам.** Если бы главный поток каждый тик присылал
«посчитай на такое-то время», то заблокированный главный поток остановил бы
и расчёт — то есть ровно то, от чего мы уходим. Поэтому у потока свой тикер,
а главный поток присылает только поправки: пауза, снятие паузы и позиция
звука (`syncShow`).

**ЗАМЕР — главное в этой записи.** Прошлый раз я честно написал, что
разделения тактов на лёгком проекте не видно. Теперь есть нагрузка:
4 вселенных × 160 приборов, 16 секвенсоров одновременно, тик 50 мс, по 12 с
на прогон, три прогона в обе стороны (чтобы разогрев и порядок не решали):

| Расчёт | Отправка avg | Отправка max |
|---|---|---|
| в главном потоке | 2,73 / 2,76 / 2,86 / 2,87 мс | 7,15–8,03 мс |
| в отдельном потоке | 0,69 / 0,38 / 0,70 / 0,43 мс | 4,23–11,41 мс |

Средняя ровность отправки улучшилась в **4–7 раз** и повторяется во всех
прогонах. Максимум шумный — там уже планировщик Windows, а не мы. На лёгком
проекте разницы нет и быть не должно: уносить было нечего.

Отдельно проверено то, ради чего всё делалось: главный поток держали занятым
синхронным циклом 0,6 с — поток за это время посчитал свои 12 кадров.
Прежде расчёт встал бы вместе с главным потоком.

**Что будет, если поток умрёт.** Счётчик тиков перестаёт расти → `healthy`
становится false → движок уходит в аварийное отключение (вода вниз), а поток
поднимается заново и остаётся БЕЗ воспроизведения. Угадывать, где была
середина шоу, и «продолжать» — хуже: на объекте это выглядело бы как прыжок
картинки, а вода при этом уже могла лить не туда. Аварийное отключение
получило третью причину рядом с «такт вставал» и «выход не доставляет».

**Что будет, если поток не поднимется вовсе** (раскладка файлов в собранном
приложении, политика запуска на машине объекта): движок молча считает
по-старому в главном потоке и пишет причину в журнал. Фонтан не должен
зависеть от того, завёлся ли воркер.

**По умолчанию ВЫКЛЮЧЕНО.** Включается `{"playbackWorker": true}` в
`app-config.json`. Это осознанно: ломается тут самое главное — поток в
линию, — и прежде чем включать насовсем, новый путь надо посмотреть на живом
объекте хотя бы один вечер. Записал в IDEAS отдельным пунктом.

**Сборка установщика.** Воркеру нужен свой файл рядом с `engine.cjs`:
добавлен `build:worker` (esbuild → `playback-worker.cjs`, 35 КБ), он включён
в `build` и в список файлов electron-builder. Проверил, что собранный бандл
действительно запускается как поток и считает кадры (10 тиков за 0,5 с при
такте 50 мс). Путь ищется так: переменная `FS_PLAYBACK_WORKER` →
`playback-worker.cjs` рядом → исходник `.ts` (разработка). Папка модуля
определяется двумя ветками, потому что движок живёт в двух видах сразу: tsx
запускает его как ESM (есть `import.meta.url`, нет `__dirname`), а в
установщик он попадает бандлом CommonJS (наоборот).

**Проверено:** новая самопроверка `worker-test` — 29 проверок: выбор пути и
запасной вариант, равенство кадров с расчётом в главном потоке на четырёх
сценах, порядок вселенных (перепутанные кадры вылезли бы именно там),
состояние и счётчик изменений доходят обратно, секвенсор идёт сам, расчёт при
заблокированном главном потоке, смерть потока и подъём заново, раскладка
общей памяти и seqlock, смена вселенных на ходу. Плюс typecheck, build,
smoke (3 прогона), projects 58, dirty-guard 22, license 54, musidora 67,
failsafe 14, eventlog 11, wind 126, telegram 84.

**Не проверено:** на живом объекте и в собранном установщике целиком (собрал
и запустил только сам бандл воркера, не весь установщик). Своих скриншотов
нет — в этой сессии нет браузерного инструмента.

**Чего эта развязка НЕ даёт** (чтобы не было завышенных ожиданий): если
главный поток встанет по СВОЕЙ причине — сборка мусора в нём, антивирус
заморозил процесс целиком, — отправка всё равно прервётся. Отправщик в
главном потоке, и унести его туда же нельзя: выходы на линию (COM-порт,
сокеты) живут там. Развязка убирает из пути отправки именно РАСЧЁТ, и замер
показывает, что именно он и был главной помехой под нагрузкой.
## 19.09.2026 — одна модель капли для ветра и для 3D

Заказчик: «да давай» (сводить τ с 3D) и два вопроса — учитываем ли теперь тип
форсунки при ветре и есть ли зона нечувствительности до 2 м/с.

**Ответы коротко.** Зона нечувствительности есть с прошлого захода:
`deadbandSpeed` = 2 м/с, ниже не делается ничего и ни для каких струй,
настраивается в «Настройках» полем «Порог, м/с». Тип форсунки при ветре
раньше НЕ учитывался — теперь учитывается, об этом и вся запись.

**Что нашлось по ходу — и это важнее самой работы.** Расчётов было два, и
расходились они не в мелочи. Визуализация считала калибр капли для каждой
форсунки (тип сопла × диаметр × распыление) с коэффициентом, подобранным по
замерам заказчика с натуры (15.09.2026: прямую струю 20 мм высотой 5 м при
ветре 15 м/с сносит примерно на 2 м, десятиметровую — на 4–5 м). Ветровое
ограничение считало по ОДНОМУ числу на весь объект, τ = 4 с.

Проверил на замерах: 3D даёт 1,95 м и 4,29 м — сходится. Ограничение давало
**6,5 м** для той же пятиметровой струи. То есть оно считало ветер втрое
сильнее настоящего и резало насосы куда раньше, чем требует реальность. В
комментарии к ограничению честно стояло «взято в строгую сторону», но
втрое — это не запас, это ошибка.

**Что сделано.** Модель вынесена в `shared/jetdrop.ts` и стала одна на
программу: таблица калибров по типам форсунок, аэродинамический диаметр с
плавным потолком 6 мм (крупнее капля разваливается набегающим воздухом),
поправка на скорость вылета, сцепка с ветром. 3D теперь зовёт эти функции
вместо своих копий; своя таблица калибров и три константы из него убраны.

**Тип сопла входит в расчёт насосов.** Для струи 6 м при ветре 6 м/с:
ламинарная 10 мм — сцепка 12,2 с, предел 75 %; прямая 20 мм — 14,5 с, 81 %;
веер — 13,9 с, 80 %; туман — **1,7 с, 34 %**. Туман и правда сдувает почти
сразу, и теперь это видно в пределе насоса. Между плотными струями разница
небольшая, и это не недоделка: все они крупнее предела устойчивости капли
(6 мм), а дальше калибр не растёт — так в физике, так и в 3D.

τ зависит ещё и от высоты струи: выше бьёт — быстрее вылет — мельче капли —
сильнее сносит. Без этой поправки десятиметровую струю сносило ровно вдвое
дальше пятиметровой, а на объекте — в 2,2 раза.

**Настройка «Сцепка с ветром, с» убрана.** Она спорила со схемой: сцепка
зависит от конкретного сопла, а это уже нарисовано. Вместо неё «Строгость, ×»
— множитель поверх расчёта (1,0 по умолчанию, 1,3 если на открытой площадке
видно, что сносит сильнее). Прежнее значение `tauSec` из проектов НЕ
переносится намеренно: открытие старого проекта — это исправление расчёта.

**Пороги начала снижения изменились сильно** (прямая струя 20 мм, снос 0,6 м):

| Высота | Было (τ=4 с) | Стало |
|---|---|---|
| 1 м | 6,3 м/с | 29,7 м/с |
| 2 м | 3,3 | 13,3 |
| 3 м | 2,3 | 8,3 |
| 4 м | 1,8 | 6,0 |
| 6 м | 1,2 | 3,7 |
| 10 м | 0,8 | 2,1 |
| 15 м | 0,5 | 1,4 |

Метровый фонтанчик теперь не трогается практически никогда — и правильно,
его ветром и не сносит. Пятнадцатиметровая по-прежнему ограничивается с
порога 2 м/с. Проверить на объекте стоит именно средние высоты (4–6 м).

**Предпросмотр в настройках переделан:** таблица не по абстрактным высотам,
а по НАСТОЯЩИМ форсункам схемы — имя, высота, расстояние до борта и предел
при разных скоростях, отсортированные от самой уязвимой. «Струя 6 м» без
типа сопла и без борта больше ничего не говорит: у тумана и у плотной струи
той же высоты пределы разойдутся втрое. Схема пустая — показываются опорные.

**Проверено:** typecheck, build, `wind-test` 126 проверок. Среди них — сверка
с замерами заказчика отдельным блоком: снос 1,95 м против замеренных «около
2 м», 4,29 м против «4–5 м», отношение 2,2× (поправка на скорость работает),
сцепка прямой струи 15,0 с, тумана 1,8 с, калибр и сцепка посчитаны для ВСЕХ
типов форсунок (забытый тип дал бы NaN и расчёт съехал бы молча). Плюс smoke
(3 прогона), projects 58, dirty-guard 22, license 54, musidora 67,
failsafe 14, eventlog 11, telegram 84.

Три проверки пришлось поправить — они были написаны под старую, втрое более
строгую модель (например, «при 2 м/с высокая струя срезана заметно»: теперь
при 2 м/с она срезана до 83 %, а заметно — при 4 м/с). Это ожидаемо и
правильно: проверки ловили изменение поведения.

**Не проверено:** картинка в 3D своими глазами — в этой сессии нет
браузерного инструмента. Расчёт для 3D не изменился (функции те же, числа
те же), но глазами не смотрел. И сами пороги на объекте — нужен ветреный
день и замеры.

**Известное упрощение:** ламинарная струя в модели парусит чуть СИЛЬНЕЕ
прямой (12,2 с против 14,5), потому что у неё тоньше сопло и мельче капля.
На объекте ламинарная — связный «стеклянный шнур», который держит ветер
лучше капельной струи того же калибра. Модель этого не знает; поправка
потребует замеров именно по ламинарным.
## 19.09.2026 — ветер: переделано по замечаниям заказчика

Заказчик разобрал прошлую версию по пунктам и был прав в каждом. Ниже — что
он сказал и что из этого вышло.

**«Не понимаю, что значит не хватает анемометра. Нам сейчас для отладки
этого и не надо».** Верно, и формулировка в задачах была неправильная:
выглядело так, будто без железа работа стоит. Расчёт сделан целиком и
проверяется без датчика — показание вводится руками. В IDEAS пункт
переписан: осталось прочитать скорость с прибора (адрес регистра и масштаб
зависят от модели), всё остальное готово.

**«Учитываешь ли ты, что при значении на насосе 100 из 255 реальная высота
струи будет метра два, а не шесть?»** НЕ учитывал — и это была главная
ошибка. Прежний код брал ПАСПОРТНУЮ высоту форсунки, считал по ней процент
и множил на него ТЕКУЩЕЕ значение. Пятнадцатиметровая форсунка, работающая
на 40 из 255 (струя меньше полуметра, ветру её не достать), срезалась до
четверти впустую.

Теперь считается текущая высота: `H = maxHeight · (значение/255)²`. Именно
квадрат, а не пропорция: у центробежного насоса напор идёт с квадрата
оборотов, а высота струи — с напора, поэтому на половине значения струя в
ЧЕТЫРЕ раза ниже, а не в полтора. Заказчик как раз это и говорил: «128 это
ровно середина струи — тут такой зависимости нет». Та же зависимость уже
зашита в 3D (там `v0 = vFull · level`, а высота идёт от `v²`), так что
картинка и ограничение теперь говорят об одном и том же.

И главное следствие: ограничение стало ПОТОЛКОМ, а не множителем. Насос,
который и так работает вполсилы, коррекция не трогает — проверено живьём
(центральный насос приглушили до 60 при действующей коррекции: остался 60).

**«Форсунки у края чаши могут капать на людей — их снос сильно критичен. Но
и центральные надо снижать: столб воды, который просто сносит, это не очень
красиво».** Сделано двумя разными пределами, которые считаются оба, и берётся
меньший:
· КАРТИНКА — ветер уводит струю от её обычного места не дальше `marginM`
  (0,6 м). Работает для ЛЮБОЙ форсунки, где бы она ни стояла.
· БОРТ — вода падает внутрь чаши с запасом `edgeReserveM` (0,3 м: вода
  падает не точкой, а пятном брызг). Расстояние до борта берётся из
  геометрии схемы: у круглой чаши по радиусу, у прямоугольной до ближней
  стенки, у вложенных ярусов — по самой тесной.
Минимальная мощность («не меньше 25%») действует только против первого. Если
вода летит за борт, «минимальная мощность» не оправдание — пусть форсунка
стоит. Живьём: центральная 15 м и приборная 3 м в 20 см от борта при 5 м/с
получили 79 и 71 из 255 — то есть режется и та и другая, приборная сильнее.

**«Форсунки могут быть наклонены кольцом вовнутрь фонтана. Ветер может
снести их в противоположную сторону, и по инерции».** Учтено формулой, а не
отдельным правилом. У наклонной струи есть начальная горизонтальная скорость
v₀, и решение для полёта даёт

    x(T) = U·T + (v₀ − U)·τ·(1 − e^(−T/τ))

При v₀ = 0 это ровно прежняя формула. Инерция — тот самый член с τ: ветер
сначала должен погасить скорость воды, направленную к центру, и только потом
начнёт сносить её наружу. Поэтому сопло, наклонённое внутрь, терпит заметно
больший ветер — и это выводится, а не назначается. Красивое следствие: уход
ОТ ВЕТРА у наклонной и вертикальной струи одинаков (наклон в разности
сокращается), поэтому предел по картинке от наклона не зависит, а по борту —
зависит. В самопроверке это закреплено отдельной строкой.

Вращающиеся и моторные насадки считаем смотрящими НАРУЖУ: их азимут меняется
на ходу, и в какой-то момент он неизбежно направлен к борту. Брать среднее
значило бы защищать среднее положение, а вода льётся в худшем.

**«Направление ветра как будто можно не учитывать... но тогда надо делать
жёсткий расчёт и учесть все моменты».** Не учитываю: считаю, что дует в
худшую сторону. Причина именно та, что заказчик и назвал — честный расчёт
требует трёх углов (направление ветра, азимут наклона, направление на
ближний борт), и ошибка в знаке любого из них означает незащищённую
форсунку. Плюс не всякий анемометр даёт направление. Записал в IDEAS как
отдельную задачу «после появления датчика с направлением».

**«Зона нечувствительности: ветер 1–2 м/с — частое явление, снижать не
совсем корректно».** Сделан порог `deadbandSpeed` = 2 м/с: ниже не делается
НИЧЕГО и ни для каких струй. Раньше порога не было вовсе — предел считался
от любой скорости, и пятнадцатиметровая струя резалась уже с 0,5 м/с.

**Времена — ровно как просил заказчик:**
· ветра не было и появился → ждём 10 с НЕПРЕРЫВНО выше порога, только потом
  начинаем коррекцию;
· внутри коррекции меняем уровень с подтверждением 1,5 с — окном минимума и
  максимума, поэтому порыв короче выдержки не двигает уровень ВООБЩЕ (не
  «почти не двигает», как было у фильтра);
· вверх — сразу, как подтвердилось (это безопасность), вниз — не быстрее
  0,5 м/с за секунду;
· ветер пропал (ниже порога) → через 10 с коррекция снимается совсем.
Плюс плавность: коррекция вводится за 3 с и снимается за 6 с, иначе струи
«ухали» бы на включении.

**Что я добавил сам и почему.** Быстрый ход при опасном ветре: выше
`stopSpeed` (12 м/с) включаемся через 3 с, а не через 10. Ждать десять
секунд разумно при обычном ветре, когда цена ошибки — некрасивая картинка;
при ветре, на котором фонтан пора глушить, десять секунд бездействия — это
лужа на дорожке. Три секунды, а не две: порывом метеорологи считают
превышение ДЛИНОЙ ОТ 3 С, значит всё короче — порыв, и реагировать нельзя
даже при опасной скорости. Выключается значением `fastSpeed` = 0.

**Что нашлось при проверке (и было исправлено):**
1. Форсунка в 20 см от борта при запасе 30 см получала НОЛЬ допустимого
   сноса и глохла от любого ветра выше порога. А кольцо форсунок по борту —
   обычная раскладка. Теперь запас берётся не больше половины того
   расстояния, что есть: такая форсунка работает, но очень приглушённо.
2. Журнал заливался: плавный ввод коррекции проходит все проценты за три
   секунды, и на каждое включение ложилось полтора десятка строк. Добавлена
   пауза 20 с между записями; начало и конец ограничения пишутся всегда.
   В живом прогоне вместо 17 строк стало 2.
3. Строка «ветер 4,8 м/с» при затихшем датчике выглядела ошибкой — теперь
   пишем оба числа: «ветер 0,5 м/с (расчётные 4,8)».
4. В строке состояния не было видно, что идёт выдержка: оператор решил бы,
   что ограничение не работает. Добавлены подписи «⏳ ждём 10 с подряд» и
   «ниже порога 2 м/с — не реагируем».

**Чего модель по-прежнему не знает** (не выдаю за истину): τ одна на объект
(в 3D она считается по калибру капли для каждой форсунки — сведение записано
в IDEAS); зависимость сноса от ветра линейна, при сильном ветре это
оптимистично (лобовое сопротивление квадратично); насос считается идеальным,
без минимальной частоты и потерь; отметка сопла `z` не учитывается — полёт
считается до уровня сопла, а у утопленного сопла он короче, то есть берём с
запасом в строгую сторону.

**Две ошибки, которые чистые тесты НЕ ловили, а живой движок поймал.** Обе в
кэше пределов: перебирать высоты для сотни насосов двадцать раз в секунду
незачем, поэтому пределы пересчитываются только при изменении расчётного
ветра или силы коррекции. Сначала я сравнивал «изменилось больше чем на
0,05» — и предел оставался устаревшим НАВСЕГДА, если последний шаг не дотягивал
до допуска: сила доходила до 0,98, потом до 1,0, разница 0,02, пересчёта нет —
насос застывал на 5 из 255 вместо нуля. Починил огрублением до 0,01 — и то же
самое повторилось на шаге 0,996 → 1,0 (застыл на 1). Вывод: любое огрубление
входа даёт право на ошибку в одну единицу DMX, а «одна единица» на стопе — это
работающий насос там, где фонтан положено глушить. Сейчас сила сравнивается
ТОЧНО (она меняется только во время плавного перехода, это секунды), ветер
огрубляется до 0,05 м/с, а переход через «стоп» вынесен в ключ отдельной
меткой. Обе ошибки закрыты проверкой на живом движке внутри `wind-test` —
через кэш проходит только он, поэтому чистые функции их и пропускали. Первая
проявлялась как плавающее падение смоука примерно раз на пять прогонов: я
полез разбираться вместо того, чтобы поднять таймаут, и потому нашёл.

**Проверено:** typecheck, build, новый `wind-test` — 111 проверок (порог,
выдержки и их прерывание, порывистая погода, подтверждение уровня, дребезг,
плавность и её монотонность, мусор с датчика — включая то, что обрыв не
считается ни ветром, ни затишьем, высота от значения, предел по текущей
высоте, геометрия чаши круглой/прямоугольной/вложенной, наклон внутрь и
наружу, час случайной погоды). Плюс smoke, projects 58, dirty-guard 22,
license 54, musidora 67, failsafe 14, eventlog 11, telegram 84.

Живьём на изолированном движке (своя сцена в памяти, рабочие данные не
тронуты): ветер 1,5 м/с полминуты — оба насоса 255; 5 м/с — десять секунд
ничего, потом плавно 188 → 131 → 79; приглушённый до 60 насос не тронут;
порыв 12 м/с на секунду расчётную скорость не сдвинул; устойчивые 9 м/с
подхвачены за 2 с; штиль — десять секунд коррекция держится, потом плавный
возврат к 255.

**Не проверено:** на живом анемометре — железа нет.
## 19.09.2026 — команды и кнопки в Telegram

Заказчик: «делай кнопки для телеграма, команды, тестируй досконально».

**Что умеет.** Смотреть: `/state` (состояние сейчас), `/report` (отчёт за
сутки) — отвечаются сразу, минуя очередь отправки: ответ на вопрос через час
не нужен никому, а очередь существует ради аварий. Управлять: `/stop` и
`/blackout`. Тишина: `/quiet 4`, `/quiet 0`; без числа бот спрашивает
кнопками, а не решает сам — четыре часа тишины это четыре часа без аварий,
и решать должен человек.

**Опасное — только через подтверждение.** `/stop` НЕ останавливает ничего:
бот присылает вопрос с кнопкой, и останавливает уже нажатие. Причина
простая: телефон в кармане не должен глушить работающий фонтан, у которого
стоят люди. В самопроверке это закреплено отдельно: ни одна текстовая
команда (`/stop`, `/стоп`, `/STOP`, `/stop@Foutain_bot`, `/stop 1`) не даёт
действия остановки.

**Кнопка «Принято» под аварией.** Нажал — кнопка сменилась отметкой
«✅ Принял Имя в 21:34», и в чат ушла строка, кто взял в работу. Ради этого
всё и просили: видно, что авария за кем-то, и двое не поедут на объект
одновременно. Длинное имя ужимается до 20 символов, иначе подпись кнопки не
влезает на телефоне.

**Права.** Команды принимаются только из чата, заданного получателем в
настройках. Постороннему не отвечаем ВООБЩЕ — даже отказом: ответ
подтвердил бы, что бот живой и чем-то управляет. Попытка пишется в журнал
не чаще раза в час (в открытой группе иначе завалит). Проверяем чат, а не
человека: чат владельца объекта и есть круг доверия, а список людей пришлось
бы вести руками и он расходился бы с реальностью. Всё выключается галочкой
«Команды из чата» в «Настройках» — это единственная дорога снаружи внутрь.

**Как устроено.** Разбор — отдельным файлом `telegramCommands.ts` чистыми
функциями: обновление → список решений («ответить», «всплывашка»,
«заменить кнопки», «выполнить действие»). Ни сети, ни движка, поэтому
проверяется сотней случаев, включая злонамеренные. Отправка и опрос — в
`telegram.ts`. Опрос длинный (`getUpdates` с timeout 25 с): человек нажал
кнопку и ждёт ответа сейчас, а не через минуту; webhook не годится — на
объекте нет внешнего адреса, а часто и интернета. У каждого запроса свой
таймаут через AbortController: без него на подвисшем модеме fetch не
вернётся никогда и команды перестанут приходить совсем.

Команды понимаются и по-русски (`/состояние`, `/отчёт`, `/стоп`, `/тихо 4`),
а в меню бота уходят латиницей с русскими описаниями: Telegram считает
командой только `/латиница`, и только такие видны списком и нажимаются.

**Проверено.** `telegram-test` — 84 проверки (было 17): разбор строк
(регистр, «ё», адресация другому боту, пробелы, аргументы), права (чужой
чат, незаданный получатель, выключенные команды), безопасность (ни одна
текстовая команда не глушит фонтан; ни одно обновление из чужого чата не
даёт действий), «Принято», тихий режим, меню команд, пределы Telegram
(callback_data ≤ 64 байт, команды только латиницей).

Живьём на @Foutain_bot: `getMe` — темы включены; `setMyCommands` принято,
`getMyCommands` вернул все шесть; сообщение с клавиатурой отправлено;
`editMessageReplyMarkup` заменил кнопки и вернул обратно; `getUpdates`
отвечает. **Не проверено:** само нажатие кнопки живым человеком — его
отсюда не сделать. Прогон команд от живого движка тоже не делал: у бота
один приёмник обновлений, и запусти я второй — Telegram отдал бы 409
конфликт работающему движку заказчика.
## 19.09.2026 — ветер: выдержка перед подъёмом, разбор порога и формулы

Заказчик: «когда ветра нет вообще, чтобы мы начали корректировать, он должен
продержаться 5–10 секунд — подумай, сколько оптимально; какая нижняя граница
в м/с, на которую мы реагируем; какую формулу применил — линейную,
нелинейную; точно ли она лучшая для нас?» Плюс: привести в порядок IDEAS.

**Выдержка перед подъёмом — сделано (`releaseHoldSec`, по умолчанию 10 с).**
Прошлая версия фильтра начинала отпускать сразу, пусть и медленно (15 с).
Этого мало: между порывами ветер проваливается почти в ноль каждые несколько
секунд, и струи «дышали» бы на каждом провале. Теперь спад сначала копится в
`belowSec`, и пока он меньше `releaseHoldSec`, расчётная скорость НЕ ДВИГАЕТСЯ
вообще; только после выдержки включается плавный подъём за `releaseSec`. Рост
по-прежнему догоняем за ~1 с и сбрасываем накопленную «тишину» — вверх спешим
всегда, это безопасность.

Почему именно 10 с, а не 5: порывом метеорологи считают превышение длиной от
3 с, а затишья между порывами в шквалистую погоду — те же единицы секунд.
Десять секунд заведомо длиннее затишья, но заметно короче настоящей перемены
погоды. Пять секунд иногда попадали бы в паузу между порывами. Величина в
настройках, диапазон 0–120 с.

Функция стала с состоянием: вместо `smoothWindSpeed(prev, …) → number` теперь
`stepWindSmoothing(state, …) → WindSmoothState`, где в состоянии и расчётная
скорость, и накопленная тишина. В движке поле `windSpeed` заменено на
`windSmoothing` плюс геттер `windCalcSpeed` (имя `windState` уже занято
публичным методом — на этом я один раз спотыкнулся).

**Нижняя граница «ветра, на который реагируем» — её нет как одного числа, и
это правильно.** Порога по скорости в коде вообще нет: считается ДОПУСТИМАЯ
ВЫСОТА струи, при которой снос не превышает `marginM` (0,6 м). Поэтому
«граница» своя у каждой высоты — при заводских 0,6 м и τ = 4 с снижение
начинается с: 1 м → 6,3 м/с, 2 м → 3,3, 3 м → 2,3, 4 м → 1,8, 6 м → 1,2,
10 м → 0,8, 15 м → 0,5 м/с. Единый порог (в старой версии — 8 м/с на весь
объект) для высоких струй опаздывал в разы.

**Формула — нелинейная.** Снос за полёт: `s = U · (T − τ·(1 − e^(−T/τ)))`,
время полёта `T = 2·√(2H/g)`. По ветру `U` это ЛИНЕЙНО, по высоте `H` —
нет: при `T ≪ τ` выходит `s ≈ U·T²/(2τ)`, то есть снос растёт примерно
пропорционально высоте, а допустимая высота падает как `1/U`. Насос режется
не пропорционально, а корнем `√(h_доп/H)`: высота идёт от квадрата напора.

**Честно о слабых местах** (записал, чтобы не выдавать за идеал):
· `τ` одна на весь объект (4 с = «вода средней плотности»), хотя у прямой
  струи 10 мм получается ~5,8 с, у веера ~3,9 с — запас взят в строгую
  сторону, защищаем самую парусящую форсунку;
· НАПРАВЛЕНИЕ ветра не учитывается — считаем, что сносит в худшую сторону;
· `marginM` одна на объект, а на самом деле у каждой форсунки своё
  расстояние до борта чаши — форсунка в центре большой чаши терпит снос
  метра, а у самого борта не терпит и 20 см;
· линейность по `U` при сильном ветре оптимистична: там начинает работать
  лобовое сопротивление, которое квадратично.
Первое, что стоит улучшить, — не формула, а `marginM` по форсунке (расстояние
до борта считается по геометрии, она у нас уже есть). Дописал в IDEAS.

**IDEAS приведён в порядок.** Из «Сейчас» убраны сделанные пункты: раздельный
такт (п.1) и публикация списка отозванных (п.4, адрес Gist прописан). На место
п.1 встал остаток — расчёт кадра в `worker_threads`, с разбором того, что в
нём придётся решить (обмен кадрами через `SharedArrayBuffer`, ручное
применяется в главном потоке, смерть worker → failsafe, часы от аудио).
Сделанное описано в «Надёжность и эксплуатация» — три записи.

**Проверено:** typecheck (0 ошибок), build, smoke — в нём проверки под
выдержку: порыв 0,3 с почти не поднял расчётный ветер (3,1 из 12 м/с),
устойчивый ветер за 3 с догнали (11,4 из 12), затишье 8 с — расчётный ветер
НЕ СДВИНУЛСЯ вовсе (11,4), затишье 40 с — упал до 1,5. Плюс все самопроверки:
projects 58, dirty-guard 22, license 54, musidora 67, failsafe 14, eventlog 11,
telegram 17.

**Не проверено:** на живом датчике ветра (Modbus) — железа нет, только
расчётные прогоны. Величины `marginM`/`τ` заводские и требуют сверки с
объектом: заказчик мерил снос с натуры, надо будет подставить его цифры.
## 19.09.2026 — сглаживание ветра и раздельный такт расчёта/отправки

Заказчик указал, что прошлый раз я ветер только ЗАПИСАЛ в задачи, а не
сделал. Справедливо — сделано сейчас, вместе с первым пунктом IDEAS.

**Ветер: сглаживание (`stepWindSmoothing` в `windlimit.ts`).** Раньше
`Engine.setWindSpeed()` применял показание мгновенно: одиночный порыв или
дребезг датчика уронил бы струи на глазах у людей и через секунду поднял
обратно. Теперь показание идёт через фильтр первого порядка с РАЗНЫМИ
постоянными: рост догоняем за ~1 с (`attackSec` — это безопасность, тянуть
нельзя), спад отпускаем за ~15 с (`releaseSec` — между порывами ветер
проваливается почти в ноль, и быстрый возврат заставил бы струи «дышать»).
Заведомо невозможные показания (>40 м/с, отрицательные) отбрасываются
целиком, а не «частично сглаживаются». Первое показание берётся как есть —
иначе на старте ограничение ползло бы с нуля. Сброс (null) действует сразу:
это явная команда человека. В журнал пишем не каждый процент, а ступени по
5% — иначе строки забили бы журнал за вечер.

Зависимость от ВЫСОТЫ струи, о которой спрашивал заказчик, была уже
сделана раньше и работает: считается допустимая высота, напор режется
корнем отношения — высокие струи режутся сильно, низкие почти не трогаются
(в смоуке: 15 м → 33%, 2 м → 90% при одном и том же ветре).

**Раздельный такт расчёта и отправки (IDEAS п.1, ◐).** Расчётчик собирает
кадр и кладёт его в `u.wire`; отправщик — отдельный тикер — отдаёт
последний готовый кадр в выходы, ничего не считая. Время расчёта ушло из
пути отправки: раньше кадр уходил ПОСЛЕ расчёта (с его задержкой), теперь —
точно в свой момент; опоздал расчёт — уйдёт повтор предыдущего кадра, и
поток не прервётся. Аварийное отключение теперь следит за ОТПРАВКОЙ (именно
она определяет, идёт ли поток в линию). В статистике два джиттера: основной
— отправки, `calcAvgJitterMs`/`calcMaxJitterMs` — расчёта; в подсказке к
строке состояния видно оба.

**Честная граница, записана в IDEAS:** Node однопоточный. Если расчёт
ЗАБЛОКИРУЕТ event loop (сборка мусора, антивирус), встанут оба такта —
разделение таймеров от этого не спасает. Полная развязка = вынести расчёт в
`worker_threads`. Предрасчёт «на секунду вперёд» — туда же: он требует, чтобы
воспроизведение стало чистой функцией времени, а консоль/ветер/служебный
свет всё равно применялись в момент отправки.

**Проверено:** typecheck, build, smoke (добавлены 6 проверок сглаживания:
порыв 0,3 с почти не поднял расчётный ветер — 3,1 из 12 м/с; устойчивый за
3 с догнали — 11,4; мусор 900 м/с и отрицательное
отброшены), все самопроверки (58/22/54/67/14/11/17), в том числе failsafe —
он завязан на перенесённую проверку отправки. Живой замер после разделения
(12 с, тик 50 мс): отправка avg 0,79 / max 12,14 мс, расчёт avg 0,72 / max
12,21 мс, тиков 241 при ожидаемых ~240. Пока расчёт лёгкий — разницы почти
нет, и это ожидаемо.

## 19.09.2026 — отзыв лицензии проверен на живом HTTP-канале

Заказчик спросил, можно ли поднять публичный список отозванных бесплатно
на GitHub Gist и проверить, работает ли отзыв. Gist создать не вышло: `gh`
на машине не установлен, а анонимные Gist GitHub отключил — нужен вход
владельца аккаунта. Поэтому механику проверил на НАСТОЯЩЕМ HTTP-канале:
поднял локальный сервер, отдающий тот же самый `{"revoked": [...]}`, и
настоящий движок с `revocationUrl` в `app-config.json`.

Прошло 6 из 6 проверок, полный цикл продавца:

1. список пуст → лицензия действует (access=max);
2. ID компьютера попал в список → доступ закрыт (access=none, причина
   «Лицензия отозвана», а не «не активирована»), список лёг в кэш на диск;
3. «хостинг» выключен, сети нет → отзыв ДЕРЖИТСЯ по последнему известному
   списку, движок только пишет в лог «список не обновлён (нет сети?)»;
4. ID убран и список переопубликован → доступ вернулся сам.

То есть не хватает ровно одного — публичного адреса. Когда он появится,
меняется одна строка в `app-config.json`, код трогать не нужно.

Заодно по вопросам заказчика:

- **Ветер: записано требование о сглаживании** (`IDEAS.md`, пункт про
  датчик). Проверил код — `Engine.setWindSpeed()` применяет показание
  МГНОВЕННО. Для ручного ввода на наладке это правильно, но с живым
  датчиком одиночный порыв или дребезг уронит струи на глазах у людей.
  Нужно: снижать, только если превышение держится ~5 с, возвращать — с
  большей выдержкой, чем снижали, и отбрасывать невозможные показания.
- **Уточнено про «три разъёма» FountanPlay:** это вывод из разбора
  протокола (коды выхода F0/F8/F9), самого интерфейса в руках не было —
  на объекте стоит свериться глазами. На новых объектах это ничего не
  ограничивает: там Art-Net/sACN и сколько угодно линий.

**Проверено:** живой отзыв 6/6 (отдельный сценарий, `scratchpad`), плюс
обычный набор — typecheck, build, smoke, все самопроверки. Реальные данные
и `license-keys/` не трогали: приватный ключ только читали, чтобы
подписать тестовую лицензию для изолированного стенда.

## 18.09.2026 — полоса о подписке, понятный keygen, разбор «раздельного такта»

Разбор вопросов заказчика по итогам вчерашнего:

- **`keygen` объяснён заново.** Формулировка «один раз в жизни проекта»
  читалась так, будто ключи надо делать на каждого клиента. Переписано:
  это «печать продавца», одна на ВЕСЬ продукт, у нас уже создана
  25.08.2026, клиентам её запускать не нужно никогда, повторный запуск =
  новая печать и мгновенная смерть всех выданных лицензий. Заодно keygen
  убран из примеров в шапке файла — чтобы не тянуло запустить.
- **Полоса о конце подписки на всех вкладках.** Раньше предупреждение жило
  только в значке шапки и внутри панели лицензии — человек, работающий во
  вкладках, мог его не заметить. Теперь под шапкой висит полоса: жёлтая
  при «осталось N дней», красная в льготные дни («закроется через N дн.»),
  с кнопками «Подробнее» и «Скрыть». Скрытие — только до конца суток
  (храним дату, а не флаг): назавтра полоса возвращается, насовсем
  спрятать нельзя.
- **Расписан пункт про раздельный такт** (`IDEAS.md` → «Сейчас», п.1):
  что именно разделяем (расчёт кадров и отправку), зачем (ровный поток
  вместо джиттера), почему это НЕ про «слать реже или пачками» (DMX —
  протокол состояния, кадр уходит целиком каждый такт), где физический
  потолок (~44 Гц на линию, сейчас 20 Гц), чем ограничена глубина буфера
  (не памятью, а задержкой реакции на СТОП и фейдеры — отсюда 0,2–0,5 с) и
  что придётся решать при реализации (предрасчитывать можно только
  воспроизведение; буфер строить от часов аудио).
- **Проверено по коду и записано там же:** ограничения на число вселенных
  в программе НЕТ; физический предел — три разъёма у интерфейса
  FountanPlay (четвёртая линия продублирует третий выход), по Art-Net/sACN
  предела нет. У нового объекта по умолчанию одна линия, протокол
  «USB-DMX (FountanPlay)», выход 1, такт 50 мс.
- **Отмечено, чего не хватает для отзыва лицензий** (`IDEAS.md` → «Сейчас»,
  п.4): код готов, не хватает только адреса, по которому лежит публичный
  список. Отложено сознательно — пока идёт разработка, домен и хостинг
  рано.

**Проверено:** typecheck, build, smoke, все самопроверки (58/22/54/67/14/
11/17). Полоса снята своим скриншотом на изолированном стенде и проверена
в трёх состояниях: появляется, «Скрыть» убирает её и переживает
перезагрузку страницы, а с датой «вчера» — возвращается. Рабочие данные и
реальный license-keys/ не трогали.

## 18.09.2026 — что происходит, когда подписка кончилась

**Заказчик поправил вчерашнее решение:** истёкшая подписка НЕ понижается до
Pro, а закрывает программу целиком — «не оплатил даже самый дешёвый тариф,
значит не работает». Свои объекты это не задевает: им выдаётся бессрочная
лицензия, у неё срока нет, и интернет ей не нужен (объекты стоят в подвалах
без связи). Смягчает это льготный период.

- **Льготные дни, `GRACE_PERIOD_DAYS` = 7.** Неделю после даты окончания
  программа работает полностью и настойчиво просит оплатить: значок в
  шапке ⏳ с подсказкой «закроется через N дн.», в панели лицензии — жёлтая
  плашка с остатком дней и почтой. На восьмой день — экран с тарифами.
  Причина: оплата редко проходит день в день, а в этот вечер на объекте
  может идти программа.
- **Продление от ПРЕЖНЕЙ даты** — `issue-license.ts issue --renew`: срок был
  до 10-го, оплатил 17-го — новый срок до 10-го следующего месяца, а не до
  17-го. Иначе платить с опозданием выгодно, и льгота превращается в
  бесплатную неделю каждый месяц.
- **Экран приветствия теперь говорит по-разному** с новым клиентом («вот
  тарифы») и с тем, у кого подписка кончилась («объект и настройки на месте,
  работа откроется сразу после продления») — по новому полю `expired` в
  статусе лицензии.
- **Починено попутно:** разбор аргументов CLI съедал следующий ключ, если
  флаг шёл без значения — `--renew --plan pro` молча терял план. Теперь
  флаг без значения распознаётся правильно.
- **Расчёт окна вынесен в `expiryState()`** в `license.ts` — чтобы
  самопроверка гоняла границы (последний день, первый льготный, первый
  закрытый) на НАСТОЯЩЕЙ функции, а не на своей копии той же арифметики:
  такая копия ломается вместе с кодом и ничего не ловит.

**Проверено:** typecheck, build, smoke, все самопроверки. `license-test` —
54/54 (границы льготного окна, доступ в льготный период, все переходы
уровней). Смоук пришлось поправить: он проверял «просроченная на сутки
отклонена», а теперь это льготный период — заменено на две проверки (сутки
— работает, сверх льготы — отклонена). `--renew` прогнан вручную на
изолированной папке ключей: без флага срок считается от сегодня, с флагом —
от прежней даты окончания. Рабочие данные и реальный license-keys/ не
трогали.

**Не сделано:** рассылка напоминаний клиенту наружу (почта/телефон/Telegram)
и вкладка «Аккаунт» — в `IDEAS.md`, ждёт решения, кто отправляет и где
хранить чужие контакты.

## 18.09.2026 — предупреждение о конце подписки

Заказчик: «клиент должен заранее знать, что подписка кончается, а не
столкнуться с неприятностью». Сделана локальная часть — без учётной записи
и рассылок, которые требуют отдельной инфраструктуры:

- `shared/license.ts`: `daysUntilExpiry()` и порог `EXPIRY_WARNING_DAYS = 14`
  (у продавца свой срок — `issue-license.ts list` показывает «требуют
  внимания» за 30 дней, чтобы успеть написать первым).
- Шапка: за две недели до конца значок лицензии становится ⏳ с пульсацией
  и подсказкой «Подписка заканчивается через N дн.».
- Панель лицензии: жёлтая плашка «Остаётся N дн. — напишите нам заранее,
  чтобы продлить: <почта>» и что именно останется после окончания.
- Контакты продавца вынесены в `plans.ts` (`VENDOR_CONTACTS`) — нужны и на
  экране приветствия, и в этой плашке; телефон/Telegram/ВК ждут значений.

**Проверено:** typecheck, build, smoke, все самопроверки (`license-test`
43/43 — добавлен счёт дней: бессрочная, 10 дней, последний день, просрочка,
битая дата, порог). Плашка снята своим скриншотом на изолированном стенде
с лицензией, истекающей через 5 дней: значок ⏳, подсказка и текст на месте,
подписи влезают. Рабочие данные пользователя не трогали.

**Не сделано (ждёт решения заказчика):** вкладка «Аккаунт» с контактами
клиента и рассылка напоминаний на почту/телефон/Telegram — это отдельная
подсистема (хранение чужих персональных данных + канал отправки), и пока
непонятно, кто шлёт: приложение клиента само или продавец из своего списка.

## 18.09.2026 — уровни подписки: Pro/Max, экран приветствия с тарифами

**Зачем:** заказчик решил продавать не «лицензия/нет лицензии», а два
тарифа — Pro (эксплуатация готового фонтана) и Max (полная разработка).
Плюс нужен был экран для совсем новой установки: сейчас человек, впервые
поставивший программу, видел пустой интерфейс без объяснений.

- **shared/license.ts:** `LicensePayload.plan?: 'pro' | 'max'` (необязательное
  поле — старые лицензии без него грандфазерятся в max, ничего не ломаем
  задним числом) и новый `AccessLevel = 'none' | 'pro' | 'max'` в
  `LicenseStatus.access` — то, что РЕАЛЬНО открыто в редакторе, отдельно от
  «есть валидная лицензия»:
  - **none** — лицензии не было никогда: ни одной рабочей вкладки, только
    экран приветствия;
  - **pro** — плейлисты, шоу, расписание. Сюда же падает ЛЮБАЯ лицензия,
    которая ИСТЕКЛА (независимо, была она pro или max) — фонтан не должен
    резко остаться совсем без управления из-за забытого продления, он
    продолжает играть по расписанию то, что уже настроено;
  - **max** — полный доступ.
  - Отзыв (см. вчерашний licenseRevocation.ts) — умышленно СТРОЖЕ истечения
    срока: падает сразу до none, а не до pro — это осознанное решение
    продавца о нарушителе, а не забытое продление.
  - Логика вынесена в чистую функцию `accessFor(check, plan)` в
    `engine/license.ts` специально ради самопроверки: гонять все переходы
    (валидна/истекла/чужая подпись × pro/max/нет плана) не требуя настоящего
    приватного ключа вендора.
- **issue-license.ts:** `--plan pro|max` теперь обязателен при `issue`.
  Добавлен `--days <N>` — свой срок в днях вместо готового пресета
  (`--duration` и `--days` не одновременно). `list`/`import` показывают
  уровень подписки.
- **UI:** `App.tsx` — три вкладочных набора вместо двух (`PRO_TABS` теперь
  включает Шоу, Плейлисты, Расписание); значок лицензии в шапке — 🔒 (none) /
  ⏳ (pro, включая просроченную) / 🔑 (max). Новый экран `WelcomeView.tsx` —
  для access:none, вместо вкладок целиком (как ProjectsView для noProject):
  приветствие, карточки тарифов (`plans.ts` — цены и описания в одном месте,
  $100/мес Pro и $1000/мес Max, скидка 20% за оплату сразу за год, отдельная
  строка про индивидуальную разработку «под ключ»), ID компьютера с
  копированием, контакты (пока только почта — телефон/Telegram/ВКонтакте
  оставлены явным TODO в коде, впишу сразу как пришлют), кнопка загрузки
  файла лицензии. `LicenseView.tsx` дополнена: у истёкшей лицензии — понятное
  «доступны воспроизведение и расписание, как на Pro» вместо голого «не
  активирована», и предложение перейти на Max с ценой.
- **Решено НЕ делать сейчас:** отдельный «только для чтения» режим внутри
  редакторов Шоу/Плейлистов для тарифа Pro (запретить именно СОЗДАНИЕ/правку,
  разрешив только запуск) — это big, рискованная переделка 2000+-строчного
  ShowView, и до сих пор Плейлисты/Расписание без лицензии тоже были
  полностью редактируемыми (решение до сегодняшнего дня), так что это не
  регресс, а сознательно отложенная более тонкая настройка.

**Проверено:** typecheck, build, smoke, все самопроверки (`license-test`
37/37 — добавлены переходы accessFor на всех сочетаниях, отзыв даёт access
none). CLI (`--plan`, `--days`, отказ при обоих сразу) прогнан вручную на
изолированной тестовой папке ключей. Экран приветствия и обновлённая панель
лицензии сняты своими скриншотами в трёх состояниях (нет лицензии, активный
Pro со вкладками Шоу/Плейлисты/Расписание, модалка с апселлом на Max) —
подписи влезают, цены и уровень считаются верно. **Настоящая лицензия
пользователя проверена read-only вызовом `loadLicenseStatus` на реальной
папке данных — она (старая, без поля plan) по-прежнему проходит и
грандфазерится в max, ничего не сломано.**

**Не проверено:** реальная покупка/оплата — вся эта часть офлайновая и
ручная (продавец сам выпускает файл после переписки), платёжного шлюза нет
и не планировался.

## 18.09.2026 — разбор вопросов про лицензии: почему ломалось, что не отследить

Пользователь спросил, почему на одном и том же компьютере переставала
работать лицензия, и почему в журнале нет лицензии, выданной на другой
ноутбук раньше. Разобрался и нашёл обе причины — обе объективные, не баг
сегодняшнего дня:

- **Почему «ломалось» на своём компьютере:** до многопроектной переделки
  (17.09.2026) лицензия лежала рядом с `fountain.project.json`, то есть в
  `C:\fountain-studio\fountain.license.json`. После переделки лицензия
  привязана не к проекту, а к ПРОГРАММЕ (это и раньше было верно по смыслу —
  один компьютер может открывать много объектов), и переехала в данные
  программы: `%APPDATA%\Fountain Studio\fountain.license.json`. Старый файл
  остался на месте, но движок его больше не ищет — отсюда «лицензия
  пропала», хотя компьютер тот же самый. Проверил по датам изменения на
  диске — ровно так и есть. Старый файл не удалял, только зафиксировал.
- **Почему лицензия на другой ноутбук не видна в `list`:** учёт (журнал
  `issued-licenses.json`) появился в `issue-license.ts` только 16.09.2026 —
  до этого инструмент подписывал файл и всё, никакого следа не оставлял.
  Всё, что выпущено раньше 16.09, в журнал не попало и само туда не
  попадёт — это не потеря данных задним числом, журнала тогда просто не
  было. Если сохранился САМ файл той лицензии — добавлена команда
  `import <файл.json>`: проверяет подпись настоящим ключом и дописывает
  запись в журнал задним числом. Файла не осталось — восстанавливать нечем.
- **`issue-license.ts` — переписана шапка файла** с пояснением каждой
  команды простыми словами и явным списком, где что лежит (ключ, журнал,
  имена, отозванные) — путь ВСЕГДА один и тот же, независимо от того, из
  какой папки запущена команда (путь считается от расположения самого
  файла на диске, а не от текущей директории — источник путаницы «ключ
  сгенерировался в Fountain Studio, потому что я оттуда запускал»).
- **Отзыв лицензии — ещё раз подтверждён сценарий «хостинга нет».**
  Добавлен отдельный тест: сервер вообще не отвечает (зависшее соединение,
  не отказ и не ошибка) — движок обязан оборвать ожидание сам через 8 с и
  продолжить работать как ни в чём не бывало, не имитируя отзыв. Разбор
  устройства системы (офлайн-подпись, отдельный необязательный слой отзыва,
  почему это не whitelist) — пользователю объяснено в переписке, отдельного
  документа не заводили (это концептуальный разговор, а не решение,
  меняющее архитектуру).

**Проверено:** typecheck, build, smoke, `license-test` (расширен до 24/24 —
добавлен зависший сервер). Команда `import` прогнана вручную: восстановление
из настоящего файла (подписанного реальным ключом), повторный импорт (не
дублирует), импорт мусора (отклонён). Реальный `license-keys/` пользователя
не трогали — весь `import`-тест шёл через изолированную тестовую папку ключей.

## 17.09.2026 — кнопки текущего объекта отдельно от формы создания

По замечанию пользователя: «Сохранить как…» и «Закрыть объект» относятся к
ОТКРЫТОМУ объекту, а стояли в разделе «Новый объект» вперемешку с формой
создания — сбивало с толку. Перенесены в свой блок сразу под строкой «Открыт:
…», в самый верх экрана «Проекты»; внизу в «Новом объекте» осталась только
кнопка «+ Создать проект». Проверено скриншотом.

## 17.09.2026 — несохранённые правки при переключении, выбор папки, лицензии

**Зачем:** правки экрана «Проекты» по итогам живой проверки пользователем
(«Открыть другой проект» вместо непонятного «Объект из другой папки», кнопка
выбора папки — справа, при создании нельзя было указать, куда класть объект),
плюс безопасное переключение (раньше правка, не успевшая долететь до диска,
могла молча потеряться или молча сохраниться без спроса). Отдельно — учёт и
отзыв лицензий: три срока, имя компьютера, список отозванных.

- **Диалог о несохранённых правках.** У `ProjectStore` — `isDirty`/`discard()`.
  `openProject`/`createProject`/`copyProject`/`closeProject` теперь без
  `force` отказывают, если хранилище «грязное», и просят решить: «Сохранить и
  открыть/закрыть», «Не сохранять и…», «Отмена». Работает и при переключении
  из Проводника (двойной щелчок по `.fsproj`), не только с экрана «Проекты».
  Самопроверка: `npm -w @fountain-studio/engine run dirty-guard-test` (новая,
  22/22, поднимает настоящий движок и щёлкает по-настоящему — иначе диалог не
  проверить: сервер отвечает только тому клиенту, кто спросил).
- **Экран «Проекты» переработан:** «Открыть другой проект» (было «Объект из
  другой папки»), кнопка «Обзор…» — справа от поля пути; у форм «Создать
  проект» и «Сохранить как» — поле «Папка» с путём по умолчанию, который
  можно поменять (был жёстко зашит). У `closeProject` появился ответ об
  успехе (раньше — тишина, в UI зависал прошлый результат).
- **Попутно найдено и починено:** при совпадении имён объект получал папку
  «Новороссийск 2», но ВНУТРЕННЕЕ имя оставалось «Новороссийск» — в
  «Недавних» было не отличить два объекта с одинаковой подписью. Теперь имя
  всегда берётся от итоговой папки. Нашлось через собственный скриншот сцены
  с двумя одноимёнными объектами.
- **Учёт и отзыв лицензий** (`issue-license.ts`): добавлен пробный срок (30
  дней) — теперь `trial`/`year`/`forever`; имя компьютера отдельно от имени
  покупателя (`--device` при выпуске, `rename` — переименовать позже);
  `list` показывает всё разом и отмечает отозванные. Отзыв: `revoke`/
  `unrevoke` — локально у издателя (`revoked.json` рядом с ключом, не публично),
  `export-revocations` — публикуемый файл без имён, только ID. Движок
  (`licenseRevocation.ts`) сам подтягивает этот файл раз в час, если в
  `app-config.json` указан `license.revocationUrl`; без него ничего не делает.
  Неудачная попытка (нет сети) не трогает прежний кэш — офлайн никогда не
  выглядит как отзыв. `loadLicenseStatus()` проверяет отзыв раньше подписи и
  срока. Разбор решения — `АРХИТЕКТУРА.md` §28. Самопроверка:
  `npm -w @fountain-studio/engine run license-test` (21/21, живой HTTP-сервер
  в тесте — успех/недоступность/мусор/снятие отзыва).

**Проверено:** typecheck, build, smoke, все самопроверки (включая новые
dirty-guard-test и license-test). Экран «Проекты» снят своими скриншотами —
подписи влезают, диалог появляется по-настоящему при живом клике «Открыть».
CLI лицензий прогнан вручную на изолированной тестовой папке ключей (issue
trial/year/forever, list, rename, revoke, export-revocations, unrevoke) — все
команды отработали. Реальный `license-keys/` пользователя не трогали.

**Не проверено:** отзыв лицензии на живом объекте с реальным интернет-каналом
(revocationUrl ещё не настроен ни на одном реальном движке — публиковать
список отозванных пока негде, это решение продавца, не техническое).

## 17.09.2026 — несколько объектов на одном компьютере

**Зачем:** до сих пор проект был ОДИН на установку — второй фонтан заводился
только через экспорт/импорт архива. Теперь объект — это папка, их сколько
угодно, и переключаются они на ходу.

- **Раскладка данных разведена на две части** (как в VS Code и Unity: рабочее
  место отдельно, проект отдельно):
  - объект — самодостаточная папка `Документы\Fountain Studio\Проекты\<Имя>\`:
    `project.json` (схема, приборы, сцены, шоу, расписание), `lines.json`
    (вселенные, протокол, номера выходов, шаг тика), `audio\`, `logs\`,
    `backups\`, файл-ярлык `<Имя>.fsproj`;
  - настройки самой программы — в `%APPDATA%\Fountain Studio`: недавние
    объекты, лицензия, `fountain.secrets.json` с токеном Telegram. **Токен
    специально НЕ в папке объекта**: папку отдают коллеге, токен отдавать нельзя.
- **Движок умеет менять объект на ходу** — `packages/engine/src/projects.ts` и
  переписанный `index.ts`. Перезапуск не нужен: хранилища (`ProjectStore`,
  `AudioStore`, `AudioPlayer`, `BackupStore`, `NetworkMonitor`,
  `TelegramNotifier`, журнал) научились `rebind` на другую папку, сервер держит
  те же объекты всю жизнь процесса. Линии применяются ДО проекта — калибровка и
  насосы на Modbus индексируются по вселенным.
- **Четыре способа открыть объект:** экран «Проекты» при запуске (недавние,
  свежий под Enter), кнопка в шапке из работающей программы, двойной щелчок по
  `.fsproj` в папке объекта, аргумент `--project <папка>` (им же пользуется
  автозапуск: `tools\install-autostart.ps1 -Project "<папка>"`).
- **Одна копия программы на компьютер:** `app.requestSingleInstanceLock()` —
  вторая копия передаёт путь первой и закрывается. Иначе два движка заняли бы
  порт 9520 и стали двумя хозяевами на одной линии DMX.
- **Экран «Проекты»** (`ProjectsView.tsx`): недавние с путём и датой, пропавшие
  папки помечены, выбор папки обычным окном Windows, создание, «Сохранить
  как…» (копия объекта без журнала и бэкапов — это история исходного), закрытие
  объекта. Идёт шоу — перед переключением спрашиваем.
- **Переезд со старого расклада:** прежний `fountain.project.json` копируется
  (не переносится!) в `Проекты\<имя>\` при первом запуске новой версии. Порядок
  выбора при старте: `--project` → последний открытый → переезд → демо-объект.
- **Починено попутно:** линия без `outputs` роняла движок на
  `u.outputs.map(...)` — то есть шоу на объекте вставало из-за одного неполного
  поля в файле, который правится руками. Добавлена проверка `linesUsable()`, она
  же прикрыла импорт архива и `updateConfig` по сети.
- **Ключ лицензий убран из git** (`packages/engine/license-keys/` в
  `.gitignore`, `private.pem` снят с учёта). Сам файл на диске остался.

**Проверено:** `typecheck`, `build`, `smoke`, `projects-test` 55/55,
`musidora-test` 67/67, `failsafe-test` 14/14, `eventlog-test` 11/11,
`telegram-test` 17/17. Сверх того — живая проверка на изолированной папке
данных (рабочие файлы не трогались): 37 проверок, ноль ошибок — первый запуск,
создание второго объекта, раздельные линии, переключение без перезапуска,
закрытие, перезапуск с тем же объектом, `.fsproj`, копия, битые настройки,
`--project`. Экран «Проекты» снят своими скриншотами в двух видах (объект
открыт / объект не открыт) — подписи влезают, обрезки нет.

**Не проверено:** на живом железе ничего из этого не гоняли — нужен объект.

## 17.09.2026 — порядок коммитов и способы открыть проект

- **Правило коммитов исправлено:** один законченный запрос — один коммит,
  несколько независимых кусков — несколько коммитов. Накануне вся накопленная
  работа ушла одним коммитом на 104 файла (`56943ee`) — так больше не делаем:
  по такой истории ничего не разобрать и нельзя откатить одну правку, не задев
  остальное. Записано в `CLAUDE.md` → «Порядок сдачи работы».
- **`IDEAS.md`, пункт про несколько проектов:** расписаны способы открыть
  проект — экран недавних при запуске (последние ~10, свежий первым), «Проекты»
  из открытого приложения, и запуск из Проводника прямо из папки объекта
  (`.fsproj` по ассоциации или ярлык, через аргумент `--project <папка>`).
  Отдельно отмечено: вторую копию программы поднимать нельзя (порт 9520 и два
  хозяина на линии) — нужен одиночный запуск Electron с передачей пути первой
  копии.

## 16.09.2026 — совместная разработка, аварийное отключение, журнал на диске, Telegram

**Зачем:** к разработке подключается второй человек со своей машины; заодно
закрывали самое опасное из открытого списка.

Сделано:

- **Аварийное отключение (failsafe)** — раньше не было вообще. Движок следит,
  что кадры реально уходят на линию: если такт вставал или выход не доставляет
  дольше заданного времени (по умолчанию 10 с), насосы и клапаны принудительно
  уходят в 0, свет — по настройке. Вывод восстановился — движок сам вернулся к
  обычной картине. Настройка в проекте, состояние видно в «Настройках», уходит
  в журнал и Telegram. Проверка: `failsafe-test` (14/14).
  Важно: закрытие редактора аварией НЕ считается — шоу играет движок.
- **Журнал событий на диске** — был только в памяти и терялся при перезапуске.
  Теперь `logs/events-ГГГГ-ММ-ДД.jsonl` рядом с проектом, по файлу на сутки,
  хранение 60 дней; переживает перезапуск, нумерация продолжается, битая строка
  после обрыва питания чтение не роняет. Проверка: `eventlog-test` (11/11).
- **Telegram**: «✅ Восстановлено» при возврате узла/прибора/ПЧ на связь; тихий
  режим на 1/4/8 часов на время работ (аварии молчат, «Восстановлено» приходит
  всегда, по окончании — строка «смолчали N аварий»); имена приборов из патча
  вместо голых RDM-UID (привязка — на вкладке «Диагностика», поле `rdmUid` у
  прибора). Проверка: `telegram-test` (17/17).
- **Умолчания вывода**: новый объект заводится с ОДНОЙ линией на протоколе
  `USB-DMX (FountanPlay)`; добавленная линия повторяет протокол предыдущей и
  получает следующий номер разъёма.
- **Экспорт/импорт проекта теперь везёт и настройки линий DMX** — перенесённый
  на другой компьютер объект больше не остаётся немым.
- Диагностика драйвера FTDI: различаем «драйвера нет вовсе» (красным, надо
  ставить) и «драйвер есть, интерфейс ни разу не подключали» (жёлтым, это
  нормально) — Windows кладёт `ftd2xx.dll` только при первом подключении.
- Документы: заведены `CLAUDE.md` (правила и порядок сдачи) и этот журнал;
  `IDEAS.md` перестроен под список задач, разборы вынесены в `USB-DMX.md` и
  `АРХИТЕКТУРА.md` §27.

Не сделано / следующее:

- **Несколько проектов на одном компьютере** — первый пункт в `IDEAS.md`, там
  же развёрнутое описание того, что должно получиться.
- Проверка USB-DMX на живом интерфейсе — ждёт версии, которую не стыдно отдать
  тестировщикам на объект.

## 15–16.09.2026 — вывод на интерфейс FontanPlay (USB-DMX)

- Разобран протокол USB-интерфейса из комплекта FontanPlay (дизассемблером
  рабочей версии 1.9.2.4 с объектов пользователя). Кадр на один выход: 518 байт,
  `FF FF FF <выход> 00 <512 каналов, 255→254> FF`; `<выход>` — номер физического
  разъёма DMX (F0/F8/F9). Детали — `docs/USB-DMX.md`.
- Сделан выход «USB-DMX (FountanPlay)»: драйвер через `ftd2xx.dll` (koffi),
  запасной путь — COM-порт; в «Настройках» выбор интерфейса и выхода, под
  таблицей — живой статус (драйвер, устройства, «кадры уходят» либо причина).
  Проверка без железа: `musidora-test` (67/67).
- Найдено, почему у FontanPlay «третья вселенная налезает на первую»: она шлёт
  все три выхода безусловно, не зная числа разъёмов интерфейса. Мы шлём только
  заведённые выходы, в подсказке предупреждаем.
- Не проверено на живом интерфейсе — только на объекте.

## 15.09.2026 — 3D, подсказки, Telegram-темы

- Вода, упавшая на свою форсунку, больше не разбрызгивается: корпус покрывается
  тонкой плёнкой с рябью, вода стекает с нижней кромки.
- Верх струи не сужается «карандашом»: последняя треть растворяется по
  прозрачности и переходит в капли без видимой границы.
- Подсказка больше не перекрывает раскрытый список.
- Telegram: темы по объектам проверены вживую, раскладка работает.

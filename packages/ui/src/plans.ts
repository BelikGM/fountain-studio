/**
 * Тарифы (§27 доработки, «Продукт», 18.09.2026) — тексты и цены в одном
 * месте, чтобы экран приветствия (WelcomeView) и панель лицензии
 * (LicenseView) не расходились в формулировках.
 *
 * Цены предварительные (задал заказчик 18.09.2026, менять по мере
 * появления реальных продаж) — $100/мес Pro, $1000/мес Max, скидка 20% при
 * оплате сразу за год. Индивидуальная разработка 3D-схемы и шоу «под ключ» —
 * не тариф, отдельная договорённость (см. CUSTOM_DEV_NOTE ниже).
 */
import type { LicensePlan } from '@fountain-studio/shared';

export interface PlanInfo {
  id: LicensePlan;
  title: string;
  monthlyPriceUsd: number;
  tagline: string;
  features: string[];
  fitFor: string;
}

/** Оплата сразу за год — как у большинства подписочных сервисов. */
export const ANNUAL_DISCOUNT = 0.2;

export const PLANS: PlanInfo[] = [
  {
    id: 'pro',
    title: 'Pro',
    monthlyPriceUsd: 100,
    tagline: 'Эксплуатация готового фонтана',
    features: ['Воспроизведение готовых шоу', 'Плейлисты — программа целого вечера', 'Расписание автозапуска'],
    fitFor:
      'Подходит, если 3D-схема, приборы и шоу уже настроены — сами или нашей разработкой — и нужно просто ' +
      'управлять готовым фонтаном день за днём.',
  },
  {
    id: 'max',
    title: 'Max',
    monthlyPriceUsd: 1000,
    tagline: 'Полная разработка и наладка',
    features: [
      'Все возможности Pro',
      'Разработка новых шоу и плейлистов',
      '3D-схема фонтана, приборы и адресация',
      'Протоколы связи и диагностика оборудования',
    ],
    fitFor: 'Подходит, если сами разрабатываете программы фонтана, меняете схему или добавляете оборудование.',
  },
];

/** Не тариф — отдельная договорённость, показывается отдельной строкой под тарифами. */
export const CUSTOM_DEV_NOTE =
  'Нужна разработка 3D-схемы и шоу «под ключ» нашими силами? Это личное время разработчика — обсуждается ' +
  'индивидуально, стоимость по проекту.';

/**
 * Контакты продавца — в одном месте, потому что нужны и на экране
 * приветствия, и в панели лицензии (там предупреждение «пора продлить»).
 *
 * TODO(георгий): пришлите телефон, Telegram и ВКонтакте — впишу сюда, и они
 * появятся на экране приветствия сами (пустые строки просто не рисуются).
 */
export const VENDOR_CONTACTS = {
  email: 'belikgeorgij4@gmail.com',
  phone: '',
  telegram: '',
  vk: '',
};

export const VENDOR_EMAIL = VENDOR_CONTACTS.email;

export function priceLine(plan: PlanInfo): string {
  const annual = Math.round(plan.monthlyPriceUsd * 12 * (1 - ANNUAL_DISCOUNT));
  return `$${plan.monthlyPriceUsd}/мес, или $${annual}/год при оплате сразу (−${Math.round(ANNUAL_DISCOUNT * 100)}%)`;
}
